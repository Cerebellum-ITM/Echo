# Progress Tracker

## Current Phase

**Phase 1 — Foundation**: scaffolding, theme system, header, and basic REPL prompt.

## Current Goal

Unit 14 (meta-commands: `theme`, `logo`, `version`, `stage`) — único pendiente
del plan original. Unit 15 (banner) entregada: banner del header con dos estilos
figlet del wordmark `echo` (soundwave / shadow) elegidos al azar y coloreados
por el stage activo.

## In Progress

_(siguiente: Unit 14 — meta-commands. Fix deploy-build-muting: el builder de `deploy` dentro de `sequence --remote`/`--from` no atenuaba los commits ya desplegados porque `runDeployBuild` pasaba `deployedSet=nil` al picker (confundía "persistir marcas" —correcto deshabilitar en build— con "mostrar atenuados" —solo lee historial). Fix: resolver el target en modo solo-lectura (`resolvePullRemote(opts.Cfg, opts.From)`) y cargar `config.LoadDeployedSHAs` para el muting; `allowMark` sigue en `false`; degrada a sin-muting si no hay target resoluble. `internal/cmd/build_deploy.go` + CHANGELOG; build/test verdes. Fix i18n19 Odoo 19 (rama `fix/i18n19-conf-perms`, apilada sobre 59): DOS bugs de i18n en Odoo 19. (1) Permisos: `writeContainerConf` (`internal/cmd/i18n.go`) copiaba el `odoo.conf` efímero con `docker cp` → `root:root 0600`, ilegible/irremovible para el usuario `odoo` no-root en `/tmp` sticky → `error: config file … not readable` (exit 2) + `rm -f` cleanup `exit 1`; al fallar quedaba el `.po` viejo. Fix: escribir el conf DENTRO del contenedor por stdin (`sh -c 'cat > …'` vía `docker.ExecWithStdin`), propiedad de `odoo`, como ya hacía el pull remoto. (2) addons_path: el `-c <conf>` REEMPLAZA al conf real, y el generado no traía `addons_path` → módulos del proyecto `not installable, skipped`/`not loaded` → `.po` incompleto + el ERROR marcaba `failed`. Fix: el conf ahora incluye el `addons_path` real (leído crudo del `odoo.conf` del contenedor con `extractAddonsPath`, sin filtrar enterprise) vía `odoo.RenderConf(conn, addonsPath)`; el pull remoto pasa el `addons_path` del perfil. Solo 19+. Tests `i18n_conf_test.go` (no-op legacy + `extractAddonsPath`) y `RenderConf` actualizado. PENDIENTE (acordado, va para largo): (a) streamear en vivo los logs de los comandos i18n, (b) comando/flag debug que imprima el `odoo.conf` que Echo genera. Unit 59 shell-run en rama `feat/59-shell-run` (apilada sobre `feat/58`): nuevo comando `shell-run [<archivo>]` que corre un `.py` por el Odoo shell vía stdin (`odoo shell -d <db> --no-http < script.py`, `exec -T` sin TTY) y auto-copia la salida (`--no-copy` lo evita). Sin arg → picker de `.py`; los `.py` se listan de la nueva config `scripts_dir` (default raíz, top-level). Reusa `emitStreamLine`/`lastOutput`/`clipboard`, `cmd.PickOne` y el orden newest-first de recipes; piping vía nuevo `docker.ExecWithStdin` (patrón de `RestoreSQL`); builder `odoo.Shell` compartido con `RunOdooShell`; confirma en prod. Archivos: `internal/cmd/shellrun.go`, `internal/repl/shellrun.go`, `odoo.Shell`, `docker.ExecWithStdin`, config `ScriptsDir`, dispatch+Registry+commandFlags+help+CHANGELOG. Tests `shellrun_test.go` (pythonScriptsIn/resolveScriptArg) y `odoo/shell_test.go`; build/vet/test+registry cross-check verdes; verificación EN VIVO pendiente del usuario (sin docker en el entorno dev). Unit 58 restyle-fixes en rama `feat/58-restyle-logs-modules`: arregla los dos outputs de Unit 57 que NO cumplían su propio checklist. **logs** no se pintaba como `update` por DOS causas: (1) `docker compose logs -f` antepone el gutter `servicio  | ` → `--no-log-prefix` en `Logs`/`LogsFollow` (`internal/docker/compose.go`); (2) `docker compose logs` reproduce el ANSI que Odoo guardó bajo TTY (a diferencia de `update`/`install` con `exec -T`, planos), y esos SGR rompían `formatOdooLine` dejando la línea cruda con colores nativos de Odoo (logger sin pastel) → `emitStreamLine` ahora limpia ANSI con `stripANSISeq` antes de parsear (igual que `shell`), no-op para `update`. Test `logsansi_test.go` prueba que con ANSI no machea y stripeada machea idéntico a la línea plana. **modules** ahora prefija cada módulo con el glyph nerd-font `` (`cod-package`, U+EB29) en acento + nombre en `Out`, conservando wrap al ancho y count line (`renderModuleList` nuevo en `modules_list.go`; `renderMatchList` del picker intacto); test `modules_list_test.go`. build/vet/test verdes; verificación EN VIVO de `logs` pendiente del usuario (sin docker en el entorno de dev). Unit 57 consistency-restyle entregada (cierre del pass de estilo iniciado por `ps`: auditados los ~35 comandos, casi todos ya estilizados; se reestilaron los 3 que faltaban: **db-list** → tabla `name·size·created` (`cmd.DBList`+`emitDBListTable`, activa con `●` verde), **modules** → lista envuelta al ancho (`renderMatchList`) + `echo.modules: modules listed count=N` (`cmd.ModulesList`+`emitModulesList`; `--config` igual), **logs** follow → ahora pipea+escanea y colorea con `emitStreamLine` como up/down/update (`docker.LogsFollow` gana `onLine`, Ctrl+C cancela vía context); `--no-follow`/`--copy` ya coloreaban. Rama `feat/odoo19-i18n`; verificación visual pendiente). Unit 56 ps-styled-table entregada (`ps` dejó el passthrough crudo de `docker compose ps` y ahora renderiza una tabla estilizada `service·image·status·ports` —patrón de `modstate`, header en acento, `pad` compartido— leyendo estructurado vía `--format json` (`docker.PSList`/`parsePSJSON` maneja array y NDJSON; `PSContainer.Ports()` compacta puertos publicados a `pub→target`). `status` coloreado por salud/estado (`psStatusStyle`); cierra con `echo.ps: containers listed count=N`. Fallback al streaming crudo (`cmd.RunPS`) si el JSON falla → sin regresión. `internal/docker/ps.go`+test, `internal/repl/ps.go`, dispatch de `ps`/`logs` separado. Rama `feat/odoo19-i18n`; verificación visual pendiente). Unit 55 picker-log-framed entregada (reestilo "log-framed" del `fuzzyPicker` —compartido por todos los pickers— para integrarse al stream de logs: fuera el título negrita-acento y la línea `────`; el bloque cuelga de una **barra `│` izquierda coloreada por el stage** (verde dev/amarillo staging/rojo prod, default cuando no se conoce); filtro en su propia línea `filter ›` con placeholder `type to filter…` legible (`PlaceholderStyle`=Faint); filas indentadas con `splitLabel` (nombre resaltado, columna secundaria dim); cursor `❯`+selección también del color del stage; `chromeLines` 6→4. Stage cableado en TODOS los pickers locales (install/update/uninstall/test/build vía `runFuzzyPickerCore`+`cfg.Stage`, i18n module/export/update, connect user+recientes vía `target.stage`/`prof.Stage`); el picker de target queda default —su stage vive en el perfil remoto, no se sabe pre-conexión. Campos `accent`/`setAccent`, `runSingleFuzzyPickerStaged` y `stage` en `runFuzzyPickerCore`; test de `splitLabel`; rama `feat/odoo19-i18n`; verificación visual pendiente del usuario en TTY). Unit 54 system-status-line entregada (línea Odoo-style `echo.system.status: system cli=… odoo=… project=… db=…` —logger unificado vía sentinel `sub=="system"` en los wrappers— emitida una sola vez al iniciar `connect`/`run`/`i18n-pull`, no por sub-comando, lo más arriba posible (primera en run, tras resolver target en connect, y en i18n-pull apenas se lee el perfil remoto reemplazando la línea `connected`); i18n-pull quitó el `start` genérico inútil y abre con `selecting remote target`/`target resolved`; pensada para one-shot sin banner del REPL y como diagnóstico —`odoo=unknown` delata un target sin `odoo_version`. `cli` = `repl.FullVersion()` (incluye `+<sha>`/`.dirty`); para exponerla a `internal/cmd` —que no puede importar `internal/repl`— se agregó `var cmd.EchoVersion` seteada desde `main.go`. Helpers `statusProjectName`/`statusFields` en `internal/cmd/status.go` + `runStatusLog` en `internal/repl/recipe.go`; emisión en `RunConnect` (tras target resolved) y `RunI18nPull` (tras perfil remoto). Tests de los helpers; build/vet/test verdes; rama `feat/odoo19-i18n`). Tras esta también: el fix de propagación de `odooVersion` en i18n-pull (causa real del `--modules` contra Odoo 19) y soporte Ctrl+X para cerrar el REPL (incluido **dentro de los pickers** vía `cmd.ErrQuit`: el `fuzzyPicker` setea `quit`, los runners devuelven `ErrQuit`, y los helpers terminales —`finalize`/`commandFailureLog`/`readonlyFinalize`— lo detectan con `handleQuit` para romper el loop de `Start`; one-shot sale con código 0). La status-line además ganó el campo `env=<stage>`. Unit 53 odoo19-i18n-cli entregada (soporte Odoo 19 para `i18n-export`/`i18n-update`/`i18n-pull`: Odoo 19 reemplazó las flags de servidor `--modules=`/`--i18n-export=`/`--i18n-import=` por el subcomando `odoo i18n export|import`, que solo acepta `-c`/`-d` —sin `--db_*`—. Builders `odoo.ExportI18n`/`UpdateI18n` ahora version-aware con firma `(c, version, module, lang, path, confPath)`: en major≥19 emiten la forma `odoo i18n …` y las credenciales viajan en un `odoo.conf` efímero dentro del contenedor (`odoo.RenderConf` + `tmpConfInContainer`, escrito local vía `docker.CopyToContainer` / remoto vía `runSSH` + `sh -c 'cat > …'`, borrado junto al `.po`); legacy 17/18 sin cambios. Versión tomada de `cfg.OdooVersion` (local) y de `RemoteProfile.OdooVersion` propagada a `connectTarget.odooVersion` (remoto); `ParseRemoteProfile` ahora lee `odoo_version`. Helpers nuevos `odoo.Major`/`odoo.RenderConf`. Tests de argv legacy/v19 + `Major`/`RenderConf` + perfil remoto; build/vet/test verdes; rama `feat/odoo19-i18n`; pendiente prueba real contra un Odoo 19). Unit 46 shell-banner-style entregada (extiende el transform del shell al bloque de arranque del Odoo Python shell: globals env/odoo/openerp/self como key=value de Echo —clave en acento, valor dim— y el banner Python/IPython atenuado; `styleShellBanner` en `internal/repl/shellbanner.go` tras el match de log; rama `feat/migration-detection`). Fix del coloreado del shell aplicado: Odoo colorea sus propios logs bajo TTY (`exec -t`), el transform ahora limpia ANSI con `stripANSISeq` antes de matchear (commit 8ac682b). Fix adicional: el `shell` ahora también aplica el fallback de loose-severity de la Unit 36 (líneas sueltas `Warn:`/`Error:` de wkhtmltopdf → estilo Odoo con logger `report.wkhtmltopdf`); el transform encadena `renderLogLine` → `styleShellBanner` → loose-severity → verbatim, espejando `emitStreamLine`; se extrajo `renderOdooLog` (núcleo string de `emitOdooLog`) para reformatear sin imprimir. Unit 45 shell-log-colorize entregada (el `shell` colorea los logs de arranque de Odoo —que salen crudos por el PTY— con el mismo estilo por segmentos de Echo vía `renderLogLine`; transform opt-in `docker.LineTransform` hilado desde `repl` → `cmd` → `docker`, partes interactivas verbatim, captura en crudo; flush de parciales por dígito-inicial + 30ms para no laggear el eco; rama `feat/migration-detection`). Unit 44 migration-detection entregada (detección pasiva de migraciones de Odoo en el stream de `install`/`update`/`uninstall`: tras el recap se cierra con una línea `echo.<cmd>.migration` por módulo migrado — campos `module`/`version`/`phases`; colapsa pre/post/end por módulo+versión y recorta el marcador `>` de la versión; `report` replica el resumen escaneando todo el último run sin importar el filtro; rama `feat/migration-detection`). Unit 43 view-module-file entregada (`view [<mod>]` selector de archivo del módulo → `bat`/`batcat` con fallback a impresión interna; `--copy`; host o contenedor según `addons_mode`). Unit 42 modinfo-version-check entregada (`modinfo [<mod>]` compara `ir_module_module.latest_version`+`state` vs versión del manifest normalizada con `adapt_version`; veredicto `in sync`/`update pending`/`not installed`/`db ahead`; `--copy`; one-shot). Unit 41 recipe-step-silent entregada (`<paso> --silent`/`--silent=<lvl>` en `echo run` silencia la salida del paso en pantalla + `--log`, agnóstico al comando). Unit 40 run-report entregada (comando `report` consulta los logs del último run por paso/nivel; cada `echo run` persiste `last-run.json`). Unit 39 recipe-pick-file entregada (`echo run --pick` abre selector de recetas `*.echo` del directorio actual). Unit 38 module-start-line-resolved entregada (la línea de inicio de update/install/uninstall/test ya nombra los módulos resueltos: picker, `--last`, `--all`). Unit 37 recipe-run-summary entregada (`echo run <file>` cierra con un resumen por paso: estado/warnings/`took` + línea `run summary` de totales). Unit 36 loose-severity-log-fallback entregada (líneas `Warn:`/`Error:` sueltas de wkhtmltopdf et al. se reformatean a estilo Odoo con logger `report.wkhtmltopdf`; warn cuenta, error no falla el run). Unit 35 update-last-recall entregada (`update --last` repite el último update por proyecto+db, persistido en disco; picker resalta los módulos de la última corrida y el picker vacío ofrece repetirlos). Unit 34 recipe-run-log entregada (`echo run … --log[=<path>]` → transcript plano). Unit 33 module-log-level entregada (`--level` en update/install/uninstall → `--log-level` de Odoo). Unit 32 recipe-runner entregada: `echo run <file>` (o stdin con `-`) corre una receta de comandos Echo, uno por línea, fail-fast en el primer exit≠0 (o `--continue-on-error`); `internal/repl/recipe.go` (`RunRecipe` + `runRecipeSteps` puro/testeable + `parseRecipeArgs`/`parseRecipeLines`); `run` se enruta en `main.go` dentro del bloque one-shot (no es comando del REPL/Registry); footer "Scripting" en `runHelp` fuera de `helpSections`. Build/vet/tests verdes; smoke-test confirma all-pass→0, fail-fast→2 (paso siguiente saltado), continue-on-error→1, stdin→0. Unit 31 script-mode entregada: `echo <cmd> [args]` corre un comando y sale con exit code (`0/1/2/3`), reusando el frame de render/finalize del REPL en modo headless (`newSession`/`RunOnce` en `internal/repl/script.go`); guard de no-interactividad anclado en `term.IsTerminal` (`requireTTY`/`ErrNonInteractive` en `internal/cmd/interactive.go`) en pickers + confirms + init/reset → falla closed sin TTY exigiendo arg/`--force`; flag `-C/--project-dir` en `main.go`. Build/vet/tests verdes; smoke-test de exit codes ok; pendiente prueba real contra un proyecto Odoo. Resto de unidades pendientes: Unit 14 — meta-commands. Unit 30 db-neutralize entregada: `db-neutralize [name]` + `db-restore --neutralize` corren el CLI nativo `odoo neutralize` dentro del contenedor; guard rojo solo si la DB es la activa o `stage=prod`; pendiente prueba real con el contenedor. Unit 29 connect-chrome-window-modes entregada: `connect` reusa un Chrome persistente dedicado de Echo (`~/.local/share/echo/connect-chrome`, override `$ECHO_CONNECT_CHROME_PROFILE`) en vez de lanzar ventana+perfil temporal cada vez; CDP a nivel browser abre una pestaña nueva por defecto, `--new-window` abre ventana incógnito aislada (cookie propia → varios usuarios a la vez); pendiente prueba real. Unit 28 connect-session-cache entregada: `connect` cachea la sesión minteada en `~/.config/echo/connect-sessions/<key>.toml` y la reusa en el siguiente `connect <login>` (probe HTTP a `<base>/odoo`; re-minta solo si murió o pasó el TTL de 5 días), saltándose la query de usuarios y el mint; interactivo ofrece logins recientes primero; flag `--fresh` fuerza re-mint; pendiente prueba real con Odoo. Unit 26 addons-paths-from-conf entregada: cuando el escaneo host no encuentra módulos, `modules`/`install`/`update`/etc. leen el `odoo.conf` del contenedor (`conf_path`, default `/etc/odoo/odoo.conf`), parsean `addons_path`, listan los módulos dentro del contenedor y persisten `addons_mode=conf`+rutas; en modo conf se re-lee el conf en vivo. `modules --config` sigue fijando modo host. Pendiente prueba real. Unit 25 filestore-in-container entregada: el filestore se lee/escribe DENTRO del contenedor Odoo (`docker cp`), arreglando el `FileNotFoundError` del restore; pendiente prueba real. Unit 24 flag-highlight-complete entregada: resaltado de flags (conocidas=acento, desconocidas=tenue, nunca rojo) + autocomplete de flags por Tab, vía registro `commandFlags`; pendiente verificación visual en TTY. Unit 23 db-drop-force-connections entregada: `--force` en `db-drop`/`db-restore` termina las conexiones activas antes de dropear. Unit 22 odoo-native-restore entregada (db-restore acepta backups nativos de Odoo); ambas pendientes de prueba end-to-end real por el usuario. Unit 21 command-highlight verificada. Unit 18 connect-command (CDP) mergeada en 0.5.0.)_







## Completed

- [x] Unit 76 — i18n-pull-multi-module. `i18n-pull` ahora trae **varios
  módulos en una corrida**: `i18n-pull sale account es_MX`. El campo
  `i18nPullArgs.module` pasó a `modules []string`. La distinción
  módulo-vs-idioma: `--lang <code>` explícito (todos los positionales son
  módulos), o heurística de locale final (si el último positional matchea
  `isLocale` — regex `^[a-z]{2,3}(_[A-Z]{2})?(@[a-z]+)?$` — y hay ≥2
  positionales, es el idioma; si no, todos son módulos, idioma default
  `es_MX`); un solo positional sigue siendo módulo (compat Unit 50). El
  picker vacío pasó de single a **multi-select** (`runFuzzyPickerCore`,
  stage-colored). `batch := p.all || len(modules) > 1` reemplaza los checks
  `p.all` del loop: en batch un módulo que falla se salta+warn y sigue,
  cierra con `pull complete pulled/skipped`; single-módulo sigue fail-fast.
  `--all`/`--installed` sin cambios; build mode de `i18n-pull` intacto.
  Archivos: `internal/cmd/i18n_pull.go` (parser + `isLocale` + selección +
  batch), help en `repl.go`, `commandFlags["i18n-pull"]` += `--lang`. Spec
  `76-i18n-pull-multi-module.md` + fila en build plan + tests actualizados
  (`i18n_pull_test.go`: tabla multi-módulo + `TestIsLocale`) + CHANGELOG.
  Build/vet/test verdes; **pendiente verificación EN VIVO** contra un remoto.

- [x] Unit 75 — remote-test. `test <mod...> --from <target>` / `--remote`
  corre la suite de tests de Odoo en una instancia **remota**, reusando el
  mismo transporte SSH que `deploy`/`shell-run`/`logs`/`restart`
  (`resolveRemoteShell` → `remoteContainerCmd` → `runSSHStream`). La
  conexión (DB/host/credenciales) sale del **perfil remoto**, no de la
  config local; el argv de `odoo.Test` es idéntico al local (builder sin
  cambios), así que los logs se colorean igual. Los módulos se resuelven
  **antes** de ramificar (picker compartido); `--tags`/`--update` componen.
  El modo remoto **gatea en prod** vía `confirmRemoteProd` (`--force` la
  salta, sin TTY falla cerrado) — el `test` local sigue sin gatear. Los
  tokens `--from <val>`/`--remote` se despojan antes de leer positionales
  (el valor tras `--from` ya no se confunde con un módulo); nuevo parser
  puro `parseTestArgs`. `requireOdooConfig` solo se exige en modo local, así
  que el remoto corre projectless desde el repo de addons linkeado (`test`
  añadido a `projectlessOneShot` en su modo remoto). Archivos:
  `internal/cmd/test_remote.go` (`parseTestArgs` + `runTestRemote`),
  `RunTest` ramificado en `internal/cmd/modules.go`, help en `repl.go`,
  `commandFlags["test"]` += `--from`/`--remote`, `main.go`
  `projectlessOneShot`. Spec `75-remote-test.md` + fila en build plan +
  test `test_remote_test.go` (11 casos del parser) + CHANGELOG. Build/vet/
  test verdes; **pendiente verificación EN VIVO** contra un target remoto
  (muutrade→develop) por el usuario.

- [x] Unit 74 — deploy-manual-mark. El picker de `deploy` gana marcado
  manual de "desplegado": **`ctrl+d`** togglea la marca del commit bajo el
  cursor y **`ctrl+a`** marca/desmarca en masa todas las filas visibles
  (respeta el filtro). Cierra el gap de la Unit 65 (historial indexado por
  SHA exacto): rama nueva, commit rebaseado, o primer deploy desde otra
  máquina ya no aparecen falsamente como "por desplegar". Solo commits (los
  dirty no tienen SHA). Se persiste al historial del target **al confirmar
  con enter**, antes del prod-gate (sobrevive aunque se cancele el deploy);
  `esc` descarta. Build mode excluido (no resuelve target). Implementación:
  `pickerItem.markable` + casos `ctrl+d`/`ctrl+a` en `fuzzyPicker.Update`,
  `deployedNames()`/`hasMarkable()`, `runFuzzyPickerCore` ahora devuelve el
  set final de marcados; `pickDeployItems` calcula el delta (add/remove,
  removals acotadas a los SHAs mostrados) y `RunDeploy` lo persiste vía el
  nuevo `config.UpdateDeployedMarks`/`UnmarkDeployed`. Spec
  `74-deploy-manual-mark.md` + fila en build plan + tests (picker toggle/bulk,
  historial add/remove/mixed/absent) + CHANGELOG, todo en la rama de trabajo.
  Build/vet/test verdes; pendiente verificación EN VIVO en TTY por el usuario.

- [x] Unit 73 — sequence-builder. Nuevo comando `sequence`: builder de
  recetas interactivo. Picker tri-estado nuevo
  (`internal/cmd/sequence_picker.go`, `RunSequencePicker`/`SeqItem`/`SeqPick`)
  que reusa el chrome del `fuzzyPicker` sin tocarlo: un `Tab` cicla cada
  ítem `off → run → build`, badge `⟦n⟧` = orden de selección = orden de
  ejecución, glyph builder `` (Nerd Font `cod-tools` U+EB6D, familia
  Codicons como el `` de modules) en acento. Los marcados build pasan por
  `cmd.RunBuild` en modo return-only nuevo (`BuildOpts.SkipDecide`), el
  resto corren as-is. Revisión con `huh.Select`: Run / Save `.echo` / Copy /
  Cancel. Ejecución vía un loop propio en `internal/repl/sequence.go`
  (`executeSequence`) que reusa `sess.runStepCaptured` (extraído de
  `recipe.go`, compartido con `echo run`), `stepStatus`/`recapLevel`/
  `stepFields`/`fmtDur`/`stripSilent`; logger `echo.sequence` /
  `echo.sequence.step`, fail-fast por defecto (`--continue-on-error` lo
  desactiva), guarda `RunReport` (para `report`). Un paso `logs` follow se
  fuerza al final (`reorderLogsLast`/`isFollowLogs`) y el summary `sequence
  complete` se emite ANTES de entrar al follow (el `^c` no marca fallo).
  Local y remoto: `--remote`/`--from <target>` (parseo `remoteRunFlags`)
  filtran la lista a remote-capables (`restart`/`logs`/`i18n-pull`/`deploy`)
  y hornean el flag remoto en cada paso (`bakeRemote`); cada comando usa su
  propio code-path remoto (cero SSH propio del sequence). `sequence --last`
  repite la última secuencia EJECUTADA del proyecto, persistida en
  `~/.config/echo/last-sequences/<key>.toml`
  (`config.LastSequence`/`Load`/`Save`); headless, sin TTY (como `run
  --last`); solo la acción Run persiste. Wiring: `Registry` +
  `dispatchNames` + `commandFlags["sequence"]={--remote,--from,--last,
  --continue-on-error}` + case en `dispatchParsed` + entradas de help.
  Tests: `sequence_picker_test.go` (cycle/renumber/picks),
  `sequence_test.go` (`isFollowLogs`/`reorderLogsLast`/`bakeRemote`/guard
  allowlist↔commandFlags/`helpDescByName`). build/vet/test/gofmt verdes;
  cross-checks Registry/help/dispatch verdes; verificación EN VIVO (TTY +
  contenedor/remoto) pendiente del usuario. Spec `73-sequence-builder.md`.
- [x] Unit 71 — log-db-name-truncate. El nombre de la DB en las líneas de
  log se trunca (solo display) cuando supera `log_db_max` (config global,
  default 20) con elipsis en medio, para no causar wrap del resto de la
  línea. Helper puro `theme.MiddleTruncate(s, max)` (rune-aware, conserva
  head+tail). Aplicado en las dos rutas de render de `repl`
  (`renderOdooLog` en logemit.go para `echo.<cmd>`, `formatOdooLine` en
  logrender.go para las líneas de Odoo streameadas; var de paquete
  `logDBMax` seteada en `newSession` desde `cfg.LogDBMax`) y en el connect
  projectless (`cmd.renderOdooLogLine`/`directConnectLogger` reciben
  `dbMax` desde `cfg.LogDBMax` de `LoadGlobal`). Las versiones plain
  (`plainOdooLog`/tee del run-log) y el texto crudo streameado conservan el
  nombre completo → `copy-last` y `--log` quedan fieles. Config
  `LogDBMax`/`log_db_max` cableada en Config/globalFile/Load×2/SaveGlobal/
  Defaults/applyDefaults. Tests `TestMiddleTruncate`. build/vet/test
  verdes; verificación visual en TTY pendiente del usuario. Spec
  `71-log-db-name-truncate.md`.
- [x] Unit 70 — update-installed-picker. `update --installed` llena el
  picker desde los módulos instalados en la DB (`ir_module_module`, vía
  `docker.ModuleStates(..., installedOnly=true)`, misma consulta que
  `modstate`) en vez de solo los addons del repo, para poder descubrir y
  actualizar módulos core como `base`. `update base` explícito ya
  funcionaba (los args saltan el picker); el flag solo cambia la fuente del
  picker, el resto del flujo (`-u`, `--last`, start line, `--i18n`/
  `--level`) intacto; inerte si se combina con módulos explícitos/`--all`/
  `--last`. Nuevos `installedModules` (guard DBContainer/DBName) y
  `installedModuleNames` (puro) en `internal/cmd/modules.go`;
  `pickModulesForUpdate` gana el parámetro `installed` y titula "Installed
  modules to update". Flag en `commandFlags["update"]` + help. Test
  `TestInstalledModuleNames`. build/vet/test verdes; verificación EN VIVO
  pendiente del usuario. GIF/README en commit DOC aparte. Spec
  `70-update-installed-picker.md`.
- [x] Unit 69 — deploy-dirty-modules. `deploy` ofrece los addons con
  cambios sin commitear (dirty) como entradas seleccionables en el mismo
  picker, arriba de los commits. `gitDirtyModules` (best-effort) corre
  `git status --porcelain` → `parsePorcelainPaths` (maneja renames `->` y
  paths citados) → `dirtyModulesFromPaths` (agrupa por addon top-level vía
  `isAddonDir`, ordenado, conserva paths por módulo). `pickDeployItems`
  (reemplaza `pickDeployCommits`) lista dirty primero con label
  `~ <module>  ·  uncommitted (N files)` —distinto del label de commit
  `<sha7>  <subject>`— y separa la selección en commits + dirty. En
  `RunDeploy` los dirty seleccionados resuelven directo (`via=dirty`),
  alimentan `pathsTouchI18n`, se unen (dedup vía `seen`) al set de módulos
  antes del loop de commits, y disparan un `WARNING` único (el código
  dirty no está en el server; deploy actualiza pero no lo sube). La línea
  de selección ahora es `items selected commits=.. dirty=..`. Help de
  `deploy` actualizado. Tests `TestParsePorcelainPaths` +
  `TestDirtyModulesFromPaths` (reusa `addonsRepo`). build/vet/test verdes;
  verificación EN VIVO contra el servidor pendiente del usuario. Spec
  `69-deploy-dirty-modules.md`.
- [x] Unit 68 — db-restore-rename. `db-restore` deja renombrar la base
  antes de restaurarla. Tras el picker, si no se pasó `--as`, sale un
  `huh.Input` "Restore as" pre-llenado con `dbNameFromBackup(...)` (el
  nombre derivado): editable para acortar nombres kilométricos (dump de
  odoo.sh) que si no ensucian todos los logs, Enter acepta el default.
  `--as <name>` salta el prompt; en no-TTY (`stdinIsTTY()` false) cae al
  derivado como antes. `promptRestoreName(palette, suggested)` +
  `validateDBName` (no vacío, sin espacios) en `internal/cmd/db.go`;
  Esc/Ctrl+C → `huh.ErrUserAborted` que `runDB` ya mapea a cancelado (sin
  crear DB). El guard de DB existente (`ErrDBExists` sin `--force`) intacto.
  Help de `db-restore` actualizado. Test `TestValidateDBName`.
  build/vet/test verdes; verificación EN VIVO pendiente del usuario. Spec
  `68-db-restore-rename.md`.
- [x] Unit 67 — db-restore-progress. `db-restore` ya no trabaja en
  silencio: narra cada fase como línea INFO estilo Odoo
  (`echo.db-restore.restore`: dropping existing database / creating
  database / restoring data con `file=`/`format=` / extracting archive /
  copying filestore / neutralizing) y además streamea en vivo el paso
  largo. `docker.Restore`/`RestoreSQL` ganan un callback `onLine` (último
  arg); con él, `pg_restore` corre con `--verbose` y su stderr se escanea
  línea a línea (helper `streamStderr`, buffer 1 MB) reenviando cada una a
  `onLine` y reservando las líneas `error`/`fatal` para el mensaje de
  fallo (`joinErrDetail`) — sin volcar todo el verbose al error. En `cmd`:
  nuevo tipo `DBLogger` + campo `DBOpts.Log` (nil-safe vía `opts.log`) y
  `restoreLineLogger(opts, target)` que strip-ea el prefijo `pg_restore:`,
  descarta líneas vacías y emite cada una como `DEBUG` bajo el mismo
  logger. El REPL cablea `opts.Log` en `runDB` igual que el logger de
  `connect` (`echo.<name>.<step>`, db→`sess.cfg.DBName` por defecto).
  `db-backup`/`Dump` quedan igual (fuera de alcance). Tests
  `TestRestoreLineLogger` (+ nil-log no-panic) en `db_test.go`.
  build/vet/test verdes; verificación EN VIVO contra un contenedor
  pendiente del usuario. Spec `67-db-restore-progress.md`.
- [x] Unit 66 — db-use. Nuevo comando `db-use [name]` que cambia la base
  activa (`cfg.DBName`) del proyecto — la que `db-list` marca con `●` y el
  destino implícito de `update`/`shell`/`psql`/`modstate`/`db-admin`/etc.
  Sin arg abre picker sobre `docker.ListDatabases` (como `db-drop`); con
  nombre cambia directo tras verificar que exista (`no database named
  "<x>"`). Persiste `db_name` con `config.SaveProject(opts.Cfg)`; como
  `sess.cfg` y `DBOpts.Cfg` son el mismo `*config.Config`, el prompt toma
  la nueva DB en el siguiente render sin reiniciar. No-op reportado al
  cambiar a la ya activa (`→ <db> (already active)`). `RunDBUse` en
  `internal/cmd/db.go`; wiring en `commands.go` (Registry, sin flags) y
  `repl.go` (dispatchNames, case del dispatch, switch de `runDB`, help);
  `TestMatchPrefix` actualizado. build/vet/test verdes. Spec
  `66-db-admin-reset.md` (extendida con db-use).
- [x] Unit 66 — db-admin. Nuevo comando `db-admin [name]` en la categoría
  Database que resetea login+password del usuario `id=2` (admin de Odoo) a
  `admin`/`admin` para recuperar acceso al back office. DB destino:
  `cfg.DBName` → arg posicional → picker (igual que `db-drop`/
  `db-neutralize`). Operación pura PostgreSQL vía la maquinaria `psql`
  existente: nuevo `docker.ResetUserCredentials` corre
  `UPDATE res_users SET login='admin', password='admin' WHERE id=2
  RETURNING id` (login/password escapados con `escapeIdent`; `found` =
  output no vacío). La contraseña se guarda en texto plano a propósito —
  el crypt context por defecto de Odoo mantiene el esquema `plaintext`
  deprecado, lo verifica en el siguiente login y lo re-hashea a
  `pbkdf2_sha512` (Odoo 16–19). `RunDBAdmin`+`confirmAdminReset` en
  `internal/cmd/db.go` (constantes `adminUserID=2`/`adminLogin`/
  `adminPassword`): guarda un confirm rojo solo si `stage=prod`
  (`--force` lo saltea); la DB activa no se protege porque es el destino
  normal; si falta la fila `id=2` falla con `no user with id 2 in "<db>"`.
  Wiring en `commands.go` (Registry primero en el cluster db, `commandFlags
  ["db-admin"]={"--force"}`) y `repl.go` (dispatchNames, case del
  dispatch, switch de `runDB`, help). Test `registry_test.go`
  `TestMatchPrefix` actualizado con `db-admin`. build/vet/test verdes;
  verificación EN VIVO contra un contenedor pendiente del usuario. Spec
  `66-db-admin-reset.md`.
- [x] Unit 15 — banner-stage-colored. Reemplaza el box `ECHO` hardcodeado de
  `buildLeft` (`internal/banner/header.go`) por el wordmark `echo` renderizado
  en uno de dos estilos figlet: **B (soundwave)** Calvin S doble trazo (`╔═╗`)
  + onda `▁▂▃▅▇▅▃▂▁`, y **D (shadow)** ANSI Shadow (`█`) con degradado vertical
  + ripple `)))` opcional. El color principal sale del stage activo
  (`PromptColor`: dev→`success`, staging→`warning`, prod→`error` del tema en
  uso) y el degradado/onda se derivan en código con `theme.Lighten`/
  `theme.Darken` (mezcla por canal hacia blanco/negro) — sin hex hardcodeado,
  cumple el invariante 1 y funciona en los 4 temas. Estilo elegido al azar al
  arrancar (`math/rand`); nueva config `banner = auto|soundwave|shadow`
  (default `auto`) para fijarlo, cableada en `config` (`Banner`, load/save +
  default) y en `banner.Opts.Banner` vía `repl.go`. Guard de ancho computado
  del arte (`computeShadowWidth`): D aparece desde ~85 cols, el ripple desde
  ~95, y por debajo cae a B — nunca desborda el borde derecho del header.
  Lógica pura/testeable: `resolveBannerStyle(mode, shadowFits, coin)` y
  `renderEchoBanner(p, stage, style, withRipple)` en `internal/banner/echo.go`.
  Tests `echo_test.go` (matriz de selección + invariante de ancho por estilo) y
  `theme/shade_test.go` (límites/midpoint/no-hex). build/vet/test verdes;
  verificación visual en TTY ancho pendiente del usuario. Spec
  `15-banner-stage-colored.md`. Fuera de alcance (futuro): logos
  `planet`/`python`/`anchor` y el comando `logo` (van con Unit 14); banner
  full-width arriba (descartado, se mantiene el header de dos columnas).
- [x] Unit 65 — deploy-history. `deploy` recuerda los commits ya
  desplegados a cada target y los atenúa en el picker. Storage local
  `internal/config/deploy_history.go`:
  `~/.config/echo/deploy-history/<projectKey>.toml`, tabla targetKey →
  `{shas, saved_at}`; `DeployTargetKey(host, path)` = sha256 hex, scope
  por destino (staging ≠ prod); `LoadDeployedSHAs` best-effort (set vacío
  ante archivo ausente/corrupto), `MarkDeployed` merge+dedup+cap 1000.
  Picker (`internal/cmd/picker.go`): `pickerItem.deployed`, `deployed`
  hilado por `runFuzzyPickerCore`/`newFuzzyPicker` (callers pasan `nil`),
  estilo Faint con prioridad cursor→deployed→recent→default y leyenda
  `muted = already deployed` (`hasDeployed`). `deploy.go`: carga el set
  antes del picker (`pickDeployCommits` recibe el set, marca labels por
  SHA, mapea cancel/empty→ErrCancelled vía core), colecta `deployedShas`
  de los commits resueltos, y `MarkDeployed` solo al terminar con éxito
  (dry-run/gate-prod/paso-fallido no registran) con línea
  `echo.deploy.history: recorded deployed commits n=…`. Tests:
  `deploy_history_test.go` (target-key distinto/estable, round-trip,
  merge/dedup, per-target, empty no-op) y `picker_test.go`
  (TestNewFuzzyPickerDeployedMarking). build/vet/test verdes;
  verificación EN VIVO contra el servidor pendiente del usuario. Spec
  `65-deploy-history.md`.
- [x] Unit 64 — deploy-i18n-detect. `deploy` autodetecta cambios de
  traducción y añade `--i18n-overwrite` al run remoto de update. Al
  resolver cada commit seleccionado se lee su diff (`gitCommitPaths`,
  ahora también para los resueltos por título — `resolveCommitModule`
  devuelve los paths del fallback o `nil` y el loop los re-fetchea para los
  subject-resueltos, degradando a "sin cambio i18n" con `WARNING` si el
  diff falla) y `pathsTouchI18n(mod, paths)` marca el módulo cuando algún
  path cae bajo `<mod>/i18n/`. Si un módulo marcado queda en el set `-u`,
  el único run lleva el flag (envolviendo `odoo.InstallUpdate` con
  `odoo.WithI18nOverwrite` de Unit 49); los del set `-i` no lo disparan y
  se loguean ("no overwrite needed"). Decisión extraída a
  `i18nOverwriteDecision(force, no, detectedUpdate)` → estado
  `on/off/forced/suppressed` + bool: flags `--i18n` (fuerza) y `--no-i18n`
  (suprime), mutuamente excluyentes en `parseDeployArgs`; per-invocación,
  nada persistido. La línea de plan gana el campo `i18n=…`, visible en
  `--dry-run`. Wiring: `commandFlags["deploy"]` += `--i18n`/`--no-i18n`,
  `helpSections`. Tests `parseDeployArgs` (overrides + mutuamente
  excluyentes), `pathsTouchI18n`, `i18nOverwriteDecision`. build/vet/test
  verdes; verificación EN VIVO contra el servidor pendiente del usuario.
  Spec `64-deploy-i18n-detect.md`.
- [x] Unit 63 — shell-pipe. `shell` con stdin no-TTY entra en modo pipe
  headless: el contenido pipeado corre por el shell de Odoo vía la
  maquinaria de shell-run (`runShellPiped` en repl, reusa
  `cmd.RunShellScript` con `Stdin: os.Stdin`), local o remoto
  (`--from`/`--remote`), salida streameada estilo Odoo y SIN auto-copy (el
  pipe es dueño de la salida; `copy-last` sigue). En el REPL interactivo
  nada cambia (stdin siempre es TTY ahí). `shell-run -` lee el script de
  stdin explícito (guard: `-` con TTY falla rápido), conservando
  auto-copy. Nuevos: `docker.ExecWithStdinReader` (ExecWithStdin delega),
  `ShellScriptOpts.Stdin io.Reader` (override de ScriptPath; remoto hace
  io.ReadAll → runSSHStream), `cmd.StdinPiped()`, helper repl
  `remoteRunFlags`. Prod headless sigue exigiendo `--force` (invariante
  9). Sin flags nuevas (autodetección); smoke: pipe local fuera de
  proyecto pide proyecto, pipe --remote resuelve projectless y falla
  cerrado pidiendo --from. Spec `63-shell-pipe.md`.
- [x] Unit 62 — remote-shell. `shell --from <target>`/`--remote` abre el
  Odoo shell de la instancia remota vía `ssh -tt -o BatchMode=yes` a través
  de `docker.RunInteractive` (extraído de `ExecInteractive`: misma
  maquinaria PTY — raw mode, SIGWINCH, detección de ^C, tee-captura y
  `LineTransform`, así el shell remoto colorea sus logs de arranque igual
  que el local); requiere TTY (falla cerrado headless). `shell-run <file>
  --from/--remote` corre el `.py` LOCAL en el shell remoto: script por
  stdin de ssh vía `runSSHStream` + `remoteContainerCmd` (`exec -T`),
  preservando el auto-copy de solo-prints (`scriptOutputLines` filtra
  igual). Nuevo `remoteExecInteractive` (exec sin `-T`). Resolución
  compartida `resolveRemoteTarget` (refactor de `resolveDeployRemote`) +
  `resolveRemoteShell`/`confirmRemoteProd` en `internal/cmd/shell_remote.go`
  (la confirmación prod usa el stage del perfil REMOTO). El parser de flags
  de `shell-run` en repl ahora consume `--from <v>`/`--remote` para que el
  valor no se confunda con el nombre del script. `projectlessOneShot` ahora
  recibe args: shell/shell-run son projectless SOLO en modo remoto (local
  fuera de proyecto sigue fallando igual). Tests `shell_remote_test.go`
  (remoteFlagsIn, quoting de remoteExecInteractive); build/vet/test verdes;
  smoke: local-sin-proyecto preservado, remoto headless falla cerrado,
  script inexistente falla antes de marcar. Pendiente prueba real contra un
  servidor. Spec `62-remote-shell.md`.
- [x] Unit 61 — deploy. Nuevo comando `deploy [--from <target>] [--limit N]
  [--dry-run] [--force]` (REPL + one-shot projectless): picker multiselect
  sobre los últimos N commits (default 20) del repo local (`git log` vía
  `os/exec`, sin librería git); cada commit se mapea a su módulo por el
  esquema `[Tag] module: title` (`deploySubjectRe`, válido solo si
  `<repo>/<mod>/__manifest__.py` existe) con fallback por `git diff-tree
  --name-only` cuando toca exactamente un módulo; commits irresolubles →
  `WARNING skipped` + conteo en el resumen (nunca abortan, salvo que TODO
  quede excluido). Split install/update consultando `ir_module_module`
  remoto (`remoteModuleStates`, psql vía SSH; `installed`/`to upgrade` →
  `-u`, resto → `-i`). Plan visible (`plan update=… install=… skipped=…`),
  `--dry-run` corta ahí, stage remoto `prod` → `confirmProd` (`--force` lo
  salta). Ejecución streameada con `runSSHStream`: `compose stop` →
  `compose up -d` → un solo run Odoo `odoo.InstallUpdate` (combina `-i`/`-u`
  + `--db_*` del `.env` remoto + `--stop-after-init`), fail-fast con el paso
  nombrado; cierra `deploy complete update=N install=M skipped=K`.
  Resolución de target idéntica a i18n-pull (`--from` → `[connect]` →
  targets globales vía `pickConnectTarget` compartido). Archivos:
  `internal/cmd/deploy.go`(+test), `internal/repl/deploy.go`,
  `odoo.InstallUpdate`, wiring Registry/dispatch/flags/help/projectless.
  Tests de parseo, subject/diff-resolution (fixtures en tmpdir), split y
  `isAddonDir` path-tricks; build/vet/test verdes. Pendiente prueba real
  contra un servidor. Spec `61-deploy-command.md`.
- [x] Unit 60 — remote-link. Nuevo comando `link [<target>] [--show]
  [--rm]` (REPL + one-shot projectless, como i18n-pull): enlaza el cwd a un
  connect target global escribiendo `ssh_host`/`remote_path` en el
  `[connect]` per-project (`config.SaveProject`); sin arg → picker (uno
  solo: auto), guardado ANTES del probe (host caído = `WARNING linked but
  unreachable`, exit 0); `--show` reporta el binding + perfil remoto
  (system-status line) y stremea el `compose ps` remoto en vivo; `--rm`
  idempotente. Nueva primitiva `runSSHStream`
  (`internal/cmd/remote.go`): variante streaming de `runSSH` con pipes
  stdout/stderr → scanner por goroutine (buffer 1 MiB para tracebacks) →
  callback serializado, error con la última línea de stderr; hook
  `sshStreamCommand` para tests. Helper `remoteComposeCmd` (lifecycle
  compose remoto, complemento de `remoteExec`). Refactor:
  `i18nPullLogger` → genérico `sess.cmdOdooLogger(name)`. Archivos:
  `internal/cmd/link.go`(+test), `internal/cmd/remote.go`(+test),
  `internal/repl/link.go`, wiring completo. Smoke-test one-shot ok
  (not-linked exit 0, non-TTY exit 2). Spec `60-remote-link.md`.
- [x] Unit 52 — run-recent-ordering. El picker de `echo run --pick` ahora ordena los recipes `.echo` por fecha de creación, más reciente primero (en `echoRecipesIn`: pares `recipeEntry{name, created}` + `sortRecipesByCreation` puro — newest-first, empates alfabéticos, zero-time al final). La fecha de creación viene de `fileCreated` con build tags: `filetime_darwin.go` lee el birthtime real vía `syscall.Stat_t.Birthtimespec`, `filetime_other.go` cae a `ModTime` (Go no expone birthtime portable). Nuevo flag `echo run --last`: resuelve el `.echo` más reciente del cwd y lo corre directo, sin picker y sin TTY (apto scripts/CI); mutuamente excluyente con `--pick`/path/stdin (usage error exit 2), compone con `--continue-on-error`/`--log`; sin recipes → `no .echo recipes found in <dir>` exit 2; tras crear la sesión emite `INFO echo.run: latest recipe → <nombre>` para que el transcript muestre la resolución. `parseRecipeArgs` amplía la tupla con `last bool` (callers/tests migrados). Help: sub-fila `--last` en el footer Scripting. Tests: `TestParseRecipeArgs` con filas `--last` (solo/compose/exclusiones), `TestSortRecipesByCreation` (puro, tiempos inyectados), `TestEchoRecipesIn` migrado a aserción de membresía (el orden exacto depende del birthtime real del FS). Build/vet/tests verdes + cross-compile linux OK. Decisión del usuario (form): flag `--last`; sort key birthtime-con-fallback fue decisión propia tras aclaración del usuario. Spec `52-run-recent-ordering.md`.
- [x] Unit 51 — build-mode. Flag universal `--build` / `-b`: `<cmd> --build` compone el comando interactivamente y pregunta qué hacer (Run / Copy / Cancel). **Interception** en `dispatchParsed` (`internal/repl/repl.go`) antes del switch — solo para comandos que el switch realmente enruta (`isDispatchName`, espeja `dispatchNames`); comando desconocido cae al path normal. `stripBuildFlag` exige que `--build` sea el único argumento (extra → WARNING `echo.build` + exit 2). **Engine** `internal/cmd/build.go` (`RunBuild`/`BuildOpts`/`BuildResult`/`BuildAction`): TTY-guard primero (`requireTTY` → `ErrNonInteractive`, falla closed en receta/CI con exit 2), luego paso 1 picker(s) posicional(es) por `buildPositionals` (módulos vía `resolveModules`; db vía `docker.ListDatabases`; backup vía `listBackupFiles`; service vía `docker.ListContainers`; i18n-export/update suman input de lang prefilled `es_MX` vía `extra`), paso 2 multi-select de flags (orden de `commandFlags`, Enter sin selección = sin flags, Esc = `ErrCancelled`), paso 3 valor por flag con value-spec (`buildFlagValues`: picker para `--level`/report-levels/i18n-pull `--from`, input para `--tags`/logs `-t`/`--out`/report `--step`/db-restore `--as`; cancelar/vacío descarta solo esa flag con WARNING `flag --x skipped`), paso 4 `composeArgs` (posicionales + flags, join `=` o espacio según el parser real) y select Run/Copy/Cancel. **Wrapper** `internal/repl/build.go` (`runBuild`): filtra aliases (`buildFlagAliases`: `logs` dropea `-c`, alias de `--copy`), cablea `Warnf` → `emitOdooLog`, y actúa — `BuildRun` emite INFO `echo.build running composed command` y re-despacha (frame normal del comando), `BuildCopy` copia la línea estilo receta SIN `echo ` al clipboard, `BuildCancel`/`ErrNothingToBuild`/errores → `finalize`/WARNING con exit 2/3/1. **Highlight + Tab** (`commandhl.go`): `universalFlags = {--build, -b}` clasifican accent y completan en cualquier comando, fuera de `commandFlags` (cross-check Registry intacto). Help: línea de footer "Build mode" fuera de `helpSections`. Tests: `composeArgs`/`BuildLine`/cross-check de registries (cmd), `stripBuildFlag`/`buildFlags` alias/`BuildValueFlags`⊆`commandFlags`/universalFlags en classifyFlag+flagsWithPrefix (repl), `RunBuild` en el fail-closed table. Build/vet/tests/gofmt verdes; smoke (binario real, sin TTY): `update --build`→exit 2, `update sale --build`→exit 2 usage, `bash --build`→exit 2, receta con `update --build`→paso falla exit 2. Pendiente: smoke interactivo en TTY (pickers visibles, Run/Copy/Cancel). Decisiones menores anotadas en el spec (sentinel exportado `ErrNothingToBuild`; TTY-guard antes de nothing-to-build; input vacío = skip). Rama `feat/project-aliases`. **Follow-up (pedido por el usuario):** `i18n-pull --build` ya no queda solo-flags — builder remote-aware dedicado `internal/cmd/build_i18npull.go` (`runI18nPullBuild`): resuelve el connect target (reusa `pickPullTarget`; fallback al `[connect]` del proyecto), **hornea `--from=<target>`**, lista los módulos del remoto (`fetchRemoteProfile`+`listRemoteConfModules`) para el picker y pide el lang → compone `i18n-pull <mod> <lang> --from=<target>`. Nuevo callback `BuildOpts.Infof` (cableado a INFO `echo.build` en el repl) + adaptador `i18nPullBuildOpts` que enruta el `Log` de `I18nPullOpts` (WARNING→Warnf, resto→Infof) para que las esperas SSH no sean silenciosas. `--all`/`--installed` no se ofrecen (ignorarían el módulo elegido). Se quitó la value-spec `i18n-pull --from` de `buildFlagValues` (ahora dead) y el helper `connectTargets`. `connect` se deja solo-flags (login requiere mint/probe remoto; ya es interactivo al ejecutar). Build/vet/tests/gofmt verdes; smoke sin TTY → exit 2 antes de tocar el remoto. Decisión del usuario (form): solo i18n-pull + hornear `--from`.
- [x] Unit 50 — i18n-pull-remote. Nuevo comando `i18n-pull [<mod>] [<lang>] [--from <target>] [--all]`: exporta las traducciones de un módulo DESDE una instancia Odoo remota (por SSH, como `connect`) y escribe el `.po` en el repo LOCAL en `<addons>/<mod>/i18n/<lang>.po`. Caso de uso: alguien editó traducciones en la UI de prod/staging y las quieres bajar al repo para commitearlas. **Reusa** la maquinaria de `connect` (`resolveConnectTarget`/`fetchRemoteProfile`/`runSSH`/`shellQuote`, todo en package `cmd`) y la lógica de destino de `i18n-export` (`resolveModuleDir`/`defaultExportDest`/`tmpPathInContainer`); cero transporte nuevo. **Origen remoto** (`internal/cmd/i18n_pull.go`): por default el `[connect]` del proyecto (`ConnectSSHHost`/`ConnectRemotePath`), o un `connect_target` nombrado con `--from`; si ninguno → `ErrNoPullRemote`. El mapeo contenedor/db remoto sale del perfil Echo del server (`resolveConnectTarget` sobre una copia de cfg con el host/path elegidos), y las credenciales PG del `.env` remoto leído por SSH (`remotePullEnv`), de modo que el `odoo --i18n-export` lleva `--db_*` explícitos (mismo motivo que el local: `compose exec` salta el entrypoint). **Por módulo, 3 llamadas SSH** (`pullRemotePO`): export a `/tmp/echo-i18n-*.po` dentro del contenedor remoto (argv vía `odoo.ExportI18n`), `cat` del archivo de vuelta (stdout = bytes del .po), `rm -f` best-effort; todas como `cd <path> && <compose> exec -T <odoo> <argv...>` (`remoteContainerCmd`, cada token shell-quoted, composeCmd crudo para que "docker compose" parta en dos). **Alcance**: un módulo por default (picker si se omite, TTY-guard), `--all` barre los módulos del repo local (`listAvailableModules`); en `--all` un módulo no resuelto/fallido se salta con warning y sigue, un run de un solo módulo aflora el error; `.po` vacío se salta (no pisa el local). **Idioma** uno por corrida, default `es_MX` (con `--all` el único positional es el idioma; si no, positionals = módulo luego idioma). Destino `<addons>/<mod>/i18n/<lang>.po`, MkdirAll + overwrite. REPL `runI18nPull` (espeja `runI18n`: startLog + runStats + commandFailureLog/finalize). Wiring: `Registry`+`dispatchNames`+`dispatchParsed`+`commandFlags["i18n-pull"]={--from,--all}`+`helpSections` (i18n); one-shot (`echo i18n-pull sale es_MX`, recipe-friendly). Tests `i18n_pull_test.go`: `parseI18nPullArgs` (tabla con defaults/módulo+lang/--from/--all/errores), `resolvePullRemote` (proyecto vs target nombrado vs desconocido vs sin remoto), `remoteContainerCmd` (quoting con compose de dos palabras y rutas con espacios). Build/vet/tests/gofmt verdes; smoke confirma el error limpio sin remoto configurado. Pendiente: prueba real contra un server SSH+Odoo (export remoto → .po en el repo). Decisiones del usuario (form): comando nuevo `i18n-pull`; origen = connect del proyecto + `--from` opcional; alcance = un módulo (picker) + `--all`. Misma rama `feat/project-aliases` (se renombrará). **Follow-up (reportado por el usuario):** `i18n-pull` ya NO exige un proyecto compose local — no toca docker local, solo escribe archivos y habla con el remoto, así que como `connect` debe correr fuera de un dir con `docker-compose.yml` (caso típico: repo de addons puro cuyo Odoo vive en un server). En `main.go`, `projectlessOneShot("i18n-pull")` hace que un `FindRoot` fallido en one-shot caiga a cwd (o `-C <dir>`) en vez de salir con "not inside a project"; sin proyecto no hay `[connect]`, así que se requiere `--from <target>` (si no, `ErrNoPullRemote` limpio). Verificado: desde `/tmp` ahora llega al comando y da el error de remoto, no el de proyecto. **Follow-up 2 (reportado por el usuario):** sin `--from` `i18n-pull` ignoraba los `connect_targets` nombrados de `global.toml` y exigía `[connect]` del proyecto. Ahora cae a ellos: orden `--from` → `[connect]` del proyecto → targets globales (`pickPullTarget`: 1 → auto con línea `using connect target <name>`; varios → `runSingleFuzzyPicker` TTY-guarded; 0 → `ErrNoPullRemote`). El fallback se dispara en `RunI18nPull` solo si `resolvePullRemote` devolvió `ErrNoPullRemote` y no había `--from`. Mensaje de `ErrNoPullRemote` actualizado para mencionar `echo connect --add`. Test `TestPickPullTarget` (0/1/varios-sin-TTY). Verificado: 1 target auto-usa y procede al SSH; 2 targets sin TTY fallan closed pidiendo selección. **Follow-up 3 (reportado por el usuario, proyecto conf-mode `habitta_prod`):** `i18n-pull` resolvía módulos solo por host (`listAvailableModules`/`resolveModuleDir`), así que en conf-mode (staging sin addons en el host) daba "no modules found" aunque `modules` listaba 44 vía `resolveModules`. Arreglado: (1) la lista/picker ahora usa `resolveModules` (conf-aware, usa los `addons_paths` del odoo.conf ya persistidos por la Unit 26) igual que `modules`/`modinfo`/`view`; (2) nuevo `pullDest`: si el módulo existe en el host (dev) escribe en su carpeta real de addons (`<addons>/<mod>/i18n/<lang>.po`, flujo intacto), si no (conf-mode) cae a `<root>/<mod>/i18n/<lang>.po` relativo al cwd para poder jalarlo y commitearlo. Se quitó el early-fail de `resolveModuleDir` (un módulo no-host ya no se salta, recibe el dest cwd-relative). Test `TestPullDest` (host-hit vs conf-fallback). Build/vet/tests/gofmt verdes. Decisión propia (el usuario pidió resolverlo sin romper el flujo host de docker-cp ni preguntar): conf-aware list + dest con fallback cwd-relative. **Follow-up 4 (mismo síntoma persistía):** el usuario eligió el target remoto `habitta_prod` en el picker pero seguía "no modules found" — porque la lista la sacaba del proyecto LOCAL (`mydb`, vía `resolveModules`), no del remoto. Topología real: jalas de un remoto cuyos módulos NO están en el proyecto local. Arreglado: la lista ahora viene del REMOTO — `listRemoteModules` consulta `SELECT name FROM ir_module_module WHERE state='installed'` por SSH en el contenedor de Postgres remoto (`remoteDBCmd`, variante de `remoteExec` que apunta a `dbContainer` en vez de `odooContainer`; `remoteContainerCmd` ahora es un wrapper). Un módulo explícito se usa tal cual (sin validar local). `pullDest` sin cambios: host dir si existe local, si no cwd-relative. Tests `remoteDBCmd` (quoting al db container). Build/vet/tests/gofmt verdes. Esto supersede el wording "conf-aware local" del commit anterior (4963856) — la fuente correcta es el remoto. **Follow-up 5 (feedback del usuario: la query DB trae TODOS los módulos Odoo, solo le interesan los del proyecto que él modifica):** el DEFAULT ahora lista los módulos del PROYECTO remoto (su `addons_path`), no `ir_module_module` completo. Nuevo `listRemoteConfModules`: usa los `addons_paths` ya persistidos en el perfil remoto (los expuse en `RemoteProfile`/`ParseRemoteProfile` junto con `addons_mode`/`conf_path`), o si no están, lee el `odoo.conf` remoto (`cat conf_path` por SSH) y lo parsea con el `parseAddonsPath` existente (que ya filtra enterprise); luego lista los dirs con `__manifest__.py` bajo esas rutas dentro del contenedor remoto (mismo script one-deep que `listModulesInContainer`, vía `remoteContainerCmd`). `dedupeSortedLines` extraído (puro/testeado). La consulta a la BD (`listRemoteModules`, ir_module_module) NO se borra — queda detrás del flag `--installed` como escape hatch (el usuario pidió conservarla). Fetch del perfil cambiado de `resolveConnectTarget` a `fetchRemoteProfile` directo para traer también las rutas de addons en un solo viaje, y construir el `connectTarget` inline. Wiring: `commandFlags["i18n-pull"]` += `--installed`, sub-fila en help. Tests: `--installed` parse, `dedupeSortedLines`. Build/vet/tests/gofmt verdes. **Follow-up 6 (feedback del usuario: la salida no seguía el estilo Echo y no avisaba durante las esperas de SSH):** se reemplazó el `StreamOut` de texto plano por un logger Odoo-style como el de `connect`. `I18nPullOpts.Log func(level, sub, msg, db string, fields ...[2]string)` (+ helper `opts.log` nil-safe); el REPL lo cablea con `i18nPullLogger` → `emitOdooLog` bajo `echo.i18n-pull[.sub]`, db = la BD remota. Eventos que enmarcan los pasos lentos: `using connect target` → `target resolved` → `reading remote profile` → `connected odoo=/db=` → `listing modules source=` / `N module(s) found` → por módulo `exporting translations module=/lang=` luego `pulled module=/file=` (o `WARNING skipped reason=`), y `pull complete pulled=/skipped=` en `--all`. `pickPullTarget` ahora toma `I18nPullOpts` (usa `opts.log`/`opts.Palette`). Fetch cambiado a `fetchRemoteProfile` directo (ya estaba) para tener las rutas de addons. Tests de `pickPullTarget` migrados al nuevo logger. Verificado el estilo en smoke (líneas `echo.i18n-pull.remote: …`). Build/vet/tests/gofmt verdes.
- [x] Unit 49 — update-i18n-overwrite. Nuevo flag `update --i18n`: añade `--i18n-overwrite` de Odoo a la corrida `-u` para que las traducciones `.po` del módulo SOBRESCRIBAN los términos ya guardados en la BD (por default un `-u` recarga pero no pisa). Nuevo builder `odoo.WithI18nOverwrite(cmd, on)` (calcado de `WithLogLevel`: append `--i18n-overwrite` si on, no-op si no). `RunUpdate` (`internal/cmd/modules.go`): parsea `--i18n` en el mismo loop de `--all`/`--last` (se consume, no cae a módulos; `extractLevel` lo deja pasar), y envuelve los 4 argvs de update (`--all`, módulos pickeados/explícitos, y ambas ramas `--last` all/modules) con `odoo.WithI18nOverwrite(odoo.WithLogLevel(...), i18n)`. **Decisión clave verificada en la doc del CLI de Odoo** ([[reference-odoo-cli-docs]], 17.0 confirmado, idéntico 18/19): `-l/--language` es SOLO para `--i18n-export`/`--i18n-import`, NO filtra idiomas en un `-u`; por eso `--i18n` es booleano y sobrescribe TODOS los idiomas activos — el caso por-idioma ya lo cubre `i18n-update <mod> <lang>` (import scoped que sí respeta `-l`). No se persiste en `LastUpdate` (es elección por-corrida, a diferencia de `--level`), así que un `update --last` pelado nunca re-sobrescribe en silencio. Wiring: `commandFlags["update"]` += `--i18n`, sub-fila en `helpSections`. Tests: `odoo/cmd_test.go` `TestWithI18nOverwrite` (append una vez / no-op). Build/vet/tests/gofmt verdes; cross-checks registry/commandhl verdes. Pendiente: prueba real contra Odoo (verificar que las traducciones se pisan). Decisiones del usuario (form): flag `--i18n`, comportamiento `--i18n-overwrite` en el mismo `-u`, alcance todos los idiomas (Opción 1 simple tras explicarle que `-l` no filtra en update). En la misma rama `feat/project-aliases` (se renombrará si hace falta).
- [x] Unit 48 — project-aliases. `-C` ahora acepta un alias corto en lugar de un directorio (`echo -C habitta modstate` desde cualquier lado), sin romper `-C <dir>` (un directorio real siempre gana). Registro a nivel usuario `name → ruta_local` en `global.toml` bajo `[project_aliases]`, misma arquitectura que `[connect_targets]`. **Config** (`internal/config/config.go` + nuevo `project_alias.go`): campo `Config.ProjectAliases map[string]string` + `globalFile.ProjectAliases` (`toml:"project_aliases"`), cableado en `Load`/`LoadGlobal`/`SaveGlobal`; helpers `ValidateAliasName` (rechaza vacío/espacios/`/`/prefijo `-`/`.`/`..`), `SetProjectAlias`/`RemoveProjectAlias` (LoadGlobal→mutar→SaveGlobal, preservan el resto, calcados de `SaveConnectTarget`), `ResolveProjectAlias` (devuelve `path,source,ok`: primero `project_aliases` source=`alias`, luego un `connect_target` homónimo cuyo `remote_path` sea dir local source=`connect`), `MigrateConnectAliases` (backfill idempotente desde connect targets con ruta local, devuelve `added`/`skipped`), `ProjectAliasList` (slice ordenado). **Resolución** (`main.go`): tras `extractProjectDir`, si el valor de `-C` no es dir existente → `ResolveProjectAlias`; si resuelve, usa la ruta; si no, error de usuario exit 2. Helper `isDir`. **Comando `alias`** (`internal/cmd/alias.go` + `internal/repl/alias.go`): `alias <name>` registra el proyecto actual, `alias`/`--list` lista, `--rm <name>` borra, `--migrate` backfillea; parse con sentinela `ErrAliasUsage` (flags malas / nombre inválido → exit 2; I/O → exit 1); el REPL rinde líneas `echo.alias` (set/removed/not found/migrated/list+count) y mapea el exit code. Headless, one-shot (`echo alias --list`). **init**: 4º grupo `huh` con input opcional "Alias" (prefill = basename del root); tras `SaveProject`, si no vacío → `SetProjectAlias` (no-fatal: el proyecto ya se guardó, un alias inválido solo avisa por `StreamOut`). Icono `IconAlias` (fa-tag U+F02B). Wiring: `Registry`+`dispatchNames`+`dispatchParsed`+`commandFlags["alias"]={--list,--rm,--migrate}`+`helpSections` (Project). Tests: `config/project_alias_test.go` (validación, set/resolve/remove, fallback a connect local vs remoto, migración idempotente con `withTempConfig` que aísla `$HOME`), `cmd/alias_test.go` (errores de uso vía `ErrAliasUsage`, set+list). Build/vet/tests/gofmt verdes; smoke del binario confirma alias set→list→`-C demo` resuelve→`--rm`, y `-C nope` exit 2. Decisiones del usuario (form): comando `alias` + paso en `init`; resolución `project_aliases` luego `connect_targets` local; migración explícita `--migrate`. Nota: los dos connect targets del usuario son remotos, así que `--migrate` no crearía ninguno (rutas no locales) — el código queda listo para quien corra Echo en el server. Rama `feat/project-aliases` (off main, independiente de `feat/modstate`).
- [x] Unit 47 — modstate. Nuevo comando `modstate [--all] [--json]`: vuelca `ir_module_module` de la BD activa (`db_container`/`db_name`) — solo instaladas por default, `--all` para cualquier estado. Sin manifest-compare (a diferencia de `modinfo`); reusa el plumbing de psql. Nuevo `docker.ModuleStates`/`ModuleStateRow` (+ `parseModuleStates` puro/testeado): query `SELECT name, state, COALESCE(latest_version,''), (latest_version IS NULL) … ORDER BY name`, el flag NULL va como ÚLTIMA columna del `-At` para que el split de versión sea inequívoco; `installedOnly=!--all`. `internal/cmd/modstate.go` `RunModstate` solo consulta (sin render): guard `ErrNoDB`, parse de `--json`/`--all` (flag desconocida o posicional → error), `env.Load` para `POSTGRES_USER`. `internal/repl/modstate.go` `runModstate` decide el ruteo de salida: **`--json`** marshala `[]{name,state,version}` (version `*string` → `null` si NULL) y escribe SOLO el array a `os.Stdout` directo (bypass de `session.print`, sin ANSI, sin línea de log; `[]` para vacío); en error escribe a **stderr** vía nuevo seam `emitOdooLogTo(w, …)` (emitOdooLog ahora delega con `os.Stdout`) y stdout queda vacío. **Humano**: tabla alineada `name | state | version` (header accent bold, state coloreado por estado, version NULL → `-` dim) vía `sess.print` con `Kind:"table"` (passthrough verbatim) + línea de cierre `modules listed count/scope`. Exit codes uniformes en ambos modos vía `exitCodeFor` (usage `ErrNoDB`/flag/posicional → 2, else 1; ok 0) — se sobrescribe el código de `finalize` en la rama humana para que una flag inválida dé 2, no 1. Headless, one-shot (`echo modstate --json`, `-C`-aware). Wiring: `Registry`+`dispatchNames`+`dispatchParsed`+`commandFlags["modstate"]={--all,--json}`+`helpSections` (Modules). Tests: `postgres_test.go` (`parseModuleStates`: 3 filas con NULL/estado-con-espacio, vacío→nil, línea corta→skip), `modstate_test.go` (`ErrNoDB`, flag desconocida, posicional). Build/vet/tests verdes; smoke del binario confirma exit 2 (flag/posicional/no-config) y stdout vacío en error JSON. Pendiente: prueba real contra un Odoo (tabla poblada + `modstate --json | jq`). Decisiones del usuario (form): nombre `modstate`; silencio total en JSON al éxito; default solo-instaladas + `--all` (uniforme en tabla y JSON, sin flag `--installed`). Pregunta resuelta: `-C` solo acepta un directorio (proyectos keyed por SHA-256 de la ruta, no hay registro nombre→ruta; los `connect_targets` nombrados son solo destinos remotos de `connect`); un `-C <perfil>` sería feature aparte.
- [x] Unit 46 — shell-banner-style. Extiende el `LineTransform` del shell (Unit 45) al bloque NO-log que el Odoo Python shell imprime antes del prompt. Tras intentar el match de log Odoo (`renderLogLine` sobre la línea limpia de ANSI), el transform cae a `styleShellBanner` (nuevo `internal/repl/shellbanner.go`): (1) los globals que Odoo inyecta en el namespace —`env:`/`odoo:`/`openerp:`/`self:`, regex `shellGlobalLine`— se pintan como campo estructurado de Echo: clave en `palette.Accent` bold + `: valor` en `Dim` (espejo del trato que `keyColor` da a `module`/`modules`); (2) las líneas del banner stock de Python/IPython (`isIPythonBanner`: prefijos `Python `, `IPython `, `Type '`, `Tip: `) se atenúan (`Faint`) para que el ruido pase a segundo plano y resalte el prompt; (3) cualquier otra línea → `("", false)` → passthrough verbatim (prompt, output de eval, blancos). Wiring: el closure del `case "shell"` en `runShell` ahora intenta `renderLogLine(clean)` y si falla `styleShellBanner(clean)`. Tests `shellbanner_test.go`: matches (4 globals + 4 líneas de banner), passthrough (blank/prompt/eval/log-line) y preservación de la clave+valor en `self: res.users(1,)`. Build/vet/tests verdes; verificado el coloreado de logs por el usuario en el server (este banner pendiente de su verificación visual). Decisión del usuario (form): globals key=value + banner tenue (vs resumen de una línea vs atenuar todo).
- [x] Unit 45 — shell-log-colorize. El comando `shell` (Odoo Python shell) ahora colorea los logs de arranque de Odoo que salen crudos por el PTY (`… INFO ? odoo: …`, `odoo.modules.loading/registry: …`) con el mismo estilo por segmentos del resto de Echo (chip de nivel, logger pastel, db en acento), reusando `renderLogLine`. Antes el tee era un `io.Copy` crudo → monocromo e incoherente. Solución: transform opt-in `docker.LineTransform func(string)(string,bool)` (nuevo en `internal/docker/shell.go`); como el estilo vive en `repl` y `docker` no puede importarlo, el closure se hila como caja negra: `repl.runShell` lo construye (captura `styles`/`palette`, llama `renderLogLine`) solo para `case "shell"`, `cmd.ShellOpts.LineTransform` lo carga, `RunOdooShell` lo pasa a `ExecInteractive` (`RunBash`/`RunPsql` pasan `nil` → passthrough intacto). `ExecInteractive` gana el param `transform`; en la ruta TTY, si no es nil, sustituye `io.Copy` por `copyWithLineTransform(os.Stdout, &buf, ptmx, transform)`. Reto = interactividad: un buffer de líneas ingenuo retendría el prompt `In [1]: ` (sin `\n`) hasta que el usuario teclee Enter. Diseño: lee el PTY en una goroutine; `emitCompleteLines` drena cada línea terminada en `\n` (al `capture` va el crudo, a stdout va el estilizado o verbatim preservando el ending `\r\n`); el parcial sobrante se vacía **crudo y al instante** salvo que empiece con dígito (único arranque posible de una línea de log Odoo), que espera `partialFlushDelay`=30ms su `\n` y si no llega se vacía crudo. El eco de teclas (nunca empieza con dígito) no sufre latencia. La captura para auto-copy queda en crudo (sin ANSI) → `copy-last` limpio. Tests `internal/docker/shell_test.go` (primer test del paquete): `emitCompleteLines` (transforma log, deja pasar plano, deja el prompt como remainder, captura cruda) y `copyWithLineTransform` con `io.Pipe` (línea de log estilizada + prompt parcial vaciado). Build/vet/tests verdes; pendiente verificación visual contra un Odoo real (shell con migración/arranque). Decisión del usuario (form): colorear estilo Echo (vs silenciar `--log-level=warn` vs silenciar+resumen).
- [x] Unit 44 — migration-detection. Detección pasiva de migraciones de datos de Odoo durante `install`/`update`/`uninstall`: el callback `StreamOut` de `runModules` alimenta un `migrationTracker` (nuevo `internal/repl/migration.go`) además de `logColorer`/`runStats`, sin invocación extra a Odoo. `migrationLine` (regex `odoo\.modules\.migration: module (\S+): Running migration \[([^\]]+)\] (pre|post|end)-migration`) captura módulo/versión/fase; `observe` colapsa por `módulo+versión` (las tres fases pre/post/end → un solo registro con `phases` en orden de aparición) y recorta el marcador de rango `>` de la versión (`18.0.0.6>` → `18.0.0.6`). Tras el switch de éxito/fallo, `sess.emitMigrations(name, resolved, migs.migrations())` (nuevo en `copylast.go`) cierra el comando con una línea INFO por módulo migrado: logger `echoCommandLogger(name,resolved)+".migration"`, mensaje `migration detected`, campos `module`/`version`/`phases=pre,post,end` — estilo Odoo, junto a las propias líneas `odoo.modules.migration`. Se emite también en fallo (la migración puede correr antes de un error posterior). `report` replica: `runReport` escanea `reportTexts(rep)` (aplana todas las líneas de todos los steps del último run, ignorando el filtro `--step`/`--level` porque las migraciones son INFO y un `--min-level=warn` las ocultaría) vía `collectMigrations` y emite las mismas líneas con logger `echo.report.migration`. Tests `migration_test.go`: dedup + acumulación de fases, orden de aparición + trim de `>`, no-match. Build/vet/tests verdes; pendiente verificación contra un update real con migración. Decisiones del usuario: resumen al final del update + reflejarlo en el report de run; rama `feat/migration-detection` (pendiente de más cambios solicitados antes de merge).
- [x] Unit 43 — view-module-file. Nuevo comando `view [<mod>] [--copy]`: selector difuso de un archivo del módulo y lo muestra por `bat`/`batcat` (syntax highlight + paginado) si está en PATH, con fallback a impresión interna temada por el canal `Line` del REPL (capturada para `copy-last`). Decisión del usuario: cadena `bat → interna`, **sin** `cat` intermedio (cat no resalta ni pagina y bypassa la captura de Echo, redundante frente a la impresión interna). `internal/cmd/view.go`: `RunView` resuelve módulo (picker single-select sobre `resolveModules` si falta), localiza el base dir según `addons_mode` (`moduleBase`: host vía `os.Stat`, conf vía `docker exec test -f`), lista archivos (`moduleFiles`: host `WalkDir` salta `__pycache__`/`.git`/`*.pyc`, conf `find -type f` con strip de prefijo), picker de archivo, y lee contenido (`readModuleFile`: host `os.ReadFile`, conf `catContainer`). `ShowWithBat` (seam `lookPath` para tests) pipea el contenido por stdin con `--file-name` para que bat deduzca el lenguaje; `batBinary` prueba `bat` y luego `batcat`. `--copy` → `clipboard.WriteAll`. `internal/repl/view.go` `runView`: copia/`bat`/fallback interno + línea `echo.view` (`module`/`file`/`via=bat|internal`); errores y cancelación vía `finalize`. `--last` (solo en memoria de la sesión, `session.lastViewModule`/`lastViewFile`) reimprime el último archivo visto sin pickers vía `cmd.RunViewLast` (re-localiza base + re-lee fresco), para poder copiarlo (`view --last --copy`); store vacío → warning `no previous view this session`. Wiring: `Registry`+`commandFlags["view"]={--copy,--last}`+`dispatchParsed`+`dispatchNames`+`helpSections` (Modules). Tests `view_test.go`: `batBinary` con seam, `skipViewPath`, `moduleFiles` host sobre tempdir; `lastflag_test.go`: `stripFlag`. Build/vet/tests verdes; pendiente prueba real contra un proyecto Odoo (bat presente y ausente, host y conf).
- [x] Unit 42 — modinfo-version-check. Nuevo comando `modinfo [<mod>] [--copy]`: corre el `SELECT name, latest_version, state FROM ir_module_module WHERE name=…` (nuevo `docker.ModuleVersion`, parsea el `name|version|state` de `psql -At`, `found=false` si no hay fila) contra el `db_container`/`db_name` activos, y lo compara con la `version` del `__manifest__.py` (host vía `os.ReadFile`, conf vía nuevo `catContainer` extraído de `readContainerFile`). Normaliza el manifest con `adaptVersion` (replica `modules.adapt_version` de Odoo: antepone la serie `17.0` derivada de `cfg.OdooVersion` vía `odooSerie` si la versión es corta) para no dar falso `update pending` entre `1.3.0` y `17.0.1.3.0`; compara segmento a segmento (`compareVersions`, numérico con segmentos faltantes=0, fallback string). Veredicto (`computeStatus`): `not installed` (sin fila o `state≠installed`), `no version`, `in sync`, `update pending` (manifest>DB), `db ahead`. `internal/cmd/modinfo.go` (`RunModinfo`, picker single-select si falta el módulo, `manifestVersion` regex). `internal/repl/modinfo.go` `runModinfo`: una línea `echo.modinfo` con `module/db/state/manifest/status`, nivel WARNING para estados no-sync; `--copy` arma el reporte plano y lo copia. `--last` (solo en memoria de la sesión, `session.lastModinfoModule`) reimprime la última revisión sin picker para poder copiarla (`modinfo --last --copy`); store vacío → warning. Wiring: `Registry`+`commandFlags["modinfo"]={--copy,--last}`+`dispatchParsed`+`dispatchNames`+`helpSections` (Modules); one-shot vía `dispatchNames` (`echo modinfo <mod>`, útil en CI/recetas). Tests `modinfo_test.go`: `manifestVersion`, `odooSerie`, `adaptVersion`, `compareVersions`, `computeStatus`. Build/vet/tests verdes; pendiente prueba real contra un proyecto Odoo.
- [x] Unit 41 — recipe-step-silent. `<paso> --silent` (todo) o `--silent=<lvl>` (ese nivel y más leves, deja pasar lo más grave) en `echo run` silencia la salida del paso en pantalla Y en el `--log`. Agnóstico al comando: lo intercepta el runner alrededor de `dispatchParsed` (funciona con stop/up/update/db-* y cualquier no-interactivo; los interactivos shell/bash/psql escriben directo al TTY → no aplica). Nuevo `internal/repl/silence.go`: var de paquete `suppressLevel` (-1 inactivo, como `runLogSink`), `silentAll=99`, `outputSuppressed(level)` (rank<=suppressLevel; ranks plain0<debug1<info2<warn3<error4<crit5), `stripSilent(args)` (quita el token, devuelve clean/suppress/label/bad; reusa `levelRank`/`normalizeLevel` de report.go). Gating: `sess.print` mueve `lastOutput.Add` arriba (captura para report aunque silenciado) y hace return antes de Println+tee si `outputSuppressed(levelFromKind(Kind))`; `emitOdooLog` return temprano si `outputSuppressed(level)`. Runner: `runStep` gana param `suppress`, el closure setea `suppressLevel` alrededor del dispatch y lo resetea; `runRecipeSteps` llama `stripSilent` por paso (warn si nivel inválido → corre sin silenciar), pasa args limpios, y el recap suma `silent=<all|lvl>` (vía `stepFields`). Las líneas del runner (`step N→`, recap, summary) se emiten con suppression inactiva → siempre visibles, incluso un `--silent=warn` deja pasar ERROR/CRITICAL (un fallo en paso silencioso no se oculta). Help suma la línea `<step> --silent[=lvl]`. Tests: `TestStripSilent`, `TestOutputSuppressed`, stubs de runStep migrados a 3 args. Build/vet/tests verdes + smoke (silent oculta y deja recap; `--silent=info` deja pasar el WARNING; `report --step=1` recupera la línea silenciada). Decisiones del usuario: ambos modos (binario + por nivel); capturar igual para report; confirmado que aplica a stop/up/etc.
- [x] Unit 40 — run-report. Nuevo comando `report` consulta/copia los logs del ÚLTIMO `echo run` por paso y nivel, entre procesos. Persistencia: cada run escribe `~/.config/echo/run-logs/last-run.json` (nuevo `internal/config/run_report.go`: `RunReport`/`StepReport`/`ReportLine` en JSON vía `writeAtomic`; `SaveRunReport`/`LoadRunReport` best-effort, archivo global único). Captura en `RunRecipe`: el closure `runStep`, tras cada `dispatchParsed`, snapshotea `sess.lastOutput.Filtered(nil)` y etiqueta cada línea con su nivel (`lineLevel(text)` parsea token Odoo/loguru/loose → distingue ERROR/CRITICAL; fallback a `levelFromKind(l.Kind)` para líneas propias de Echo y herencia de traceback), luego `sess.lastOutput.Reset()` para aislar pasos (los meta no resetean). Comando `internal/repl/report.go`: `parseReportArgs` (`--step=N`, `--level`/`--min-level` mutuamente excluyentes y validados vía `normalizeLevel`, `--copy`), `filterReport` (selección de paso + filtro exacto/umbral con `levelRank`), `runReport` (carga, filtra, cabecera Odoo `echo.report`, copia plano vía `internal/clipboard` o imprime coloreado por nivel con `kindFromLevel`). Wiring: `Registry`+`dispatchNames`+`dispatch`+`commandFlags`+`helpSections` (sección Shell) — cross-checks `registry_test`/`commandhl_test` verdes; `IsScriptCommand` lo toma de `dispatchNames` → `echo report` one-shot. Tests: `config/run_report_test` (round-trip/missing), `repl/report_test` (`parseReportArgs` tabla, `filterReport` exacto vs umbral vs all + step fuera de rango, `lineLevel`). Build/vet/tests verdes + smoke end-to-end (run escribe json; `--level=warn` 1 línea tras fix de dedup; `--step --copy`; errores de validación exit 2). Decisiones del usuario: `--level` exacto + `--min-level` umbral (acepta warn/error/critical); persistir siempre; one-shot + REPL. Limitación v1 anotada: líneas loose-severity reformateadas vía emitOdooLog (Unit 36) no pasan por lastOutputBuffer → no se capturan; un solo last-run global (no por proyecto). Pendiente: verificación con un run real contra Odoo (capturar warns/errores de un update).
- [x] Unit 39 — recipe-pick-file. `echo run --pick` abre un selector single-select con las recetas `*.echo` del directorio donde corres (top-level, sin recursión) y ejecuta la elegida, sin teclear el path. `parseRecipeArgs` gana retorno `pick bool` + flag `--pick` (mutuamente excluyente con un path posicional → error `--pick takes no recipe path`); compone con `--continue-on-error` y `--log`. Nuevo `echoRecipesIn(dir)` (escaneo puro: archivos `*.echo` top-level, ignora subdirs y no-.echo, ordenado) + `pickRecipeFile(dir, palette)` que muestra `no .echo recipes found in <dir>` si no hay; reusa el picker vía nuevo export `cmd.PickOne(title, options, palette)` (wrapper de `runSingleFuzzyPicker`, guard TTY incluido). `RunRecipe`: si `pick`, resuelve el path por el selector y mapea cancel→exit 3 / no-files·non-TTY→exit 2. Footer de ayuda "Scripting" suma `--pick`. Tests: `TestParseRecipeArgs` migrado a 6-tupla + casos `--pick`/`--pick --log`/`--pick --continue-on-error`/`--pick path`(error); `TestEchoRecipesIn` (dos `.echo` ordenados, ignora `.txt`/sin-ext/dir `sub.echo`) + vacío. Build/vet/tests verdes + smoke-test (no-files→2, --pick+path→2, non-TTY→2). Decisiones del usuario: flag `--pick`; búsqueda solo en el directorio actual. Pendiente: verificación visual del picker en TTY.
- [x] Unit 38 — module-start-line-resolved. La línea de inicio de `update`/`install`/`uninstall`/`test` ahora nombra los módulos REALES, también cuando vienen del picker o de `update --last` (antes salía `echo.update.start` genérica porque `startLog` se emitía antes de resolver). Nuevo callback `ModulesOpts.OnResolve func([]string)` + helper `emitResolved(opts, resolved)` que cada `Run*` llama justo antes de `runOdoo` con el set final ({"--all"} sentinel incluido). El REPL ya no llama `startLog` para comandos de módulos; en su lugar setea `OnResolve = func(r){ sess.startResolved(name, r) }`, y `startResolved` emite la línea con logger `echoCommandLogger(name, resolved)+".start"` + campo `modules=` (vía `moduleField`, que siempre deletrea el set aunque el logger colapse a `.modules`/`.all`). `modules` (listing) conserva su `startLog` temprano. Limpieza: `startLogger` simplificado a `"echo."+name+".start"` (sin branch de módulos ni param `args`), `resolvedFromArgs` eliminado (era código muerto al mover la línea). Errores pre-resolución (ErrNoLastUpdate, picker cancelado, sin DB) no emiten línea de inicio (no hay módulos que nombrar; el finalize/fallo ya enmarca). Tests: `TestEmitResolved` (callback llamado/no-op nil) y `TestRunUpdateLastNotifiesResolved` (`--last` con registro sembrado dispara OnResolve con `[sale account]` antes de que el subprocess inexistente falle rápido). Build/vet/tests verdes; pendiente verificación visual contra Odoo real. Decisión del usuario: mostrar el/los módulos en ejecución desde el inicio (ejemplo: tras un `update --last` no sabía qué se hizo).
- [x] Unit 37 — recipe-run-summary. `echo run <file>` ahora cierra con un resumen por paso + totales, como líneas `echo.run` estilo Odoo (sin tablas ASCII). Nuevo `stepOutcome{code,errors,warnings,duration}`; el closure `runStep` en `RunRecipe` mide la duración alrededor de `dispatchParsed` y lee los contadores del último comando desde la sesión. Para exponerlos: nuevos campos `session.lastErrors`/`lastWarnings` (junto a `exitCode`), reseteados en `dispatchParsed` y fijados por los 4 terminal helpers que cargan contadores (`finalize`, `successLog`, `copyFailureLog`, `commandFailureLog`); shell/connect/readonly/cancelled cargan 0 genuino (los cubre el reset). `runRecipeSteps` reescrito: acumula resultados, marca como `skipped` los pasos no corridos bajo fail-fast, emite recap por paso (nivel por estado: ok=INFO, failed=ERROR, cancelled/skipped=WARNING; campos `cmd`/`warnings`/`errors`/`exit`/`took`) y una línea `run summary` con `steps`/`ok`/`failed`/`skipped`/`warnings`/`took`. Exit codes intactos (all-pass→0, fail-fast→código del paso que falló, continue-on-error→exitError). Helpers `stepStatus`/`recapLevel`/`stepFields`/`fmtDur`. Tests: stubs de `recipe_test.go` migrados a `stepOutcome` (asserts de exit code idénticos) + 2 nuevos que capturan las líneas `log` y verifican recap/totales (fail-fast con skipped, warnings sumados, all-pass sin campo `failed`). Build/vet/tests verdes + smoke-test del binario (`help`/`bogus`/`help`) muestra las líneas recap y `run summary steps=3 ok=2 failed=1`. Decisión del usuario: "lo que veas útil" → estado + warnings + errores/exit en fallo + duración + totales, en estilo Odoo. Pendiente: verificación con una receta real contra Odoo.
- [x] Unit 36 — loose-severity-log-fallback. Las líneas de severidad suelta que escriben a stderr herramientas como wkhtmltopdf/Qt (`Warn: Can't find .pfb for face 'Courier'`, `Error: Failed loading page`) ya no salen como texto plano sin estilo: se reformatean a estilo Odoo (timestamp + chip + logger sintético `report.wkhtmltopdf`). Nuevo `internal/repl/looselog.go`: `looseSeverity` regex `^(?i)(warning|warn|critical|crit|fatal|error|err|info|debug):\s*(.*)$`, `parseLooseSeverity` + `looseLevel` (mapea a DEBUG/INFO/WARNING/ERROR/CRITICAL), const `looseSeverityLogger`. Nuevo helper compartido `sess.emitStreamLine(lc, line)` en `repl.go` que centraliza los dos reformateadores de líneas foráneas (compose progress + loose-severity) y luego cae al clasificador por kind; `runModules` y `runDocker` lo usan (se sacó el bloque compose inline de `runDocker`). Guard de traceback: el reformateo loose se suprime mientras `lc.last` es `err`/`warn` (herencia activa), para no secuestrar un `SomeError:` final fuera de su traceback. Conteo en `runStats.wrap`: un `Warn:` suelto suma a warnings; `Error:`/`Critical:` sueltos NO suman a errors (decisión: stderr de herramientas no debe tumbar un run sano). Tests `looselog_test.go`: tabla de `parseLooseSeverity` (todas las variantes + case-insensitive + el ejemplo `.pfb` exacto + no-matches odoo/loguru/ps/prosa/`Warnings`/blank) y `TestRunStatsLooseSeverity` (warn=1, errors=0). Build/vet/tests verdes; pendiente verificación visual en TTY con un reporte real. Decisiones del usuario: reformatear a estilo Odoo (no solo colorear ni ocultar); solo WARNING cuenta.
- [x] Unit 35 — update-last-recall. `update --last` repite el último `update` de la (proyecto, db) actual sin picker ni confirmación; si lo último fue `--all`, repite `--all`. Persistencia best-effort en `~/.config/echo/last-updates/<projectKey>.toml` (tabla `db → LastUpdate{modules,all,level,saved_at}`), nuevo `internal/config/last_update.go` calcado de `connect_session.go` (TOML, `writeAtomic`, load tolerante a archivo ausente/corrupto). Se guarda **aunque el update falle** (caso de re-correr tras un error), nunca al cancelar. El `--level` previo se hereda salvo override en la repetición. Sin `--last`: el picker de `update` resalta los módulos de la última corrida en `palette.Info` + leyenda `highlighted = last update` (solo si hay alguno); confirmar el picker **sin seleccionar nada** dispara `confirmRepeatLast` (huh, lista los módulos previos) → repetir/cancelar. `update <mods>` explícito y `--all` corren directo sin confirmación. **Solo en REPL interactivo**: nuevo `ModulesOpts.Interactive` (true solo en `Start`, false en `RunOnce`/`RunRecipe`) gatea el fallback del picker vacío, así `echo run`/`echo update` no preguntan (pero sí guardan el último). Para distinguir Esc de "Enter sin selección", se extrajo `runFuzzyPickerCore` (devuelve `canceled`) del que `runFuzzyPicker` (semántica vieja: vacío/cancel → `ErrCancelled`) es un wrapper; `pickModulesInteractive` gana parámetro `recent` (install/uninstall/test pasan nil) y hay un `pickModulesForUpdate` que expone `canceled`. `commandFlags["update"]` += `--last`; sub-fila de help `--last`. Tests: `config/last_update_test.go` (round-trip modules/all/level, aislamiento por db, upsert, archivo/clave ausentes), `cmd/picker_test.go` (marcado `recent`, `hasRecent`, `confirmRepeatLast` skip sin TTY), `runFuzzyPickerCore` añadido al fail-closed. Build/vet/tests verdes; pendiente prueba real contra Odoo. Decisiones del usuario: alcance solo `update`; persistir en disco; `--last` repite `--all`; confirmación **solo** cuando el picker se confirma sin selección (no en `update <mod>` directo).
- [x] Unit 34 — recipe-run-log. `echo run <file> --log[=<path>]` captura todo el run (output plano de cada paso + líneas `echo.run`, sin ANSI) a un `.log`. Opt-in: `--log` pelado → `~/.config/echo/run-logs/<timestamp>-<receta>.log` (nuevo `config.RunLogsDir()`); `--log=<path>` → ruta explícita; sin flag no escribe nada. Captura por tee: nueva var de paquete `runLogSink io.Writer` (nil normalmente) + `teeRunLog(plain)`; `sess.print` teea `l.Text` y `emitOdooLog` teea `plainOdooLogFields` (extiende `plainOdooLog` con los `key=val`). `parseRecipeArgs` ahora devuelve `logDest`/`logEnabled` (forma `--log` y `--log=<path>`; la forma con espacio NO se soporta para no chocar con el positional de la receta). `RunRecipe.openRunLog` resuelve ruta (default vía `RunLogsDir`+`MkdirAll`+`runLogName`/`recipeLabel`, "stdin" si viene de stdin), crea el archivo (bufio), escribe header `# echo run <receta> — <RFC3339>`, fija el sink y difiere flush/close/`runLogSink=nil` + línea final `log written path=…`; un fallo al abrir solo emite WARNING y la corrida sigue sin log. Footer "Scripting" del help suma `--log[=<path>]`. Tests: `parseRecipeArgs` extendido (--log / --log= / combinado), `TestRunLogSinkTee` (tee escribe plano + `plainOdooLogFields` sin ANSI con fields). Build/vet/tests verdes; smoke-test confirma archivo plano (0 ANSI), header, fail-fast, y que sin --log no crea archivo.
- [x] Unit 33 — module-log-level. `--level <lvl>` (y `--level=<lvl>`) en `update`/`install`/`uninstall` mapea a `--log-level` nativo de Odoo. Nuevo `odoo.LogLevels` (set 17/18/19) + `odoo.WithLogLevel(cmd, level)` (append `--log-level=<lvl>` si no vacío). En `modules.go`: helper compartido `extractLevel(args)` (parsea ambas formas, look-ahead para la forma con espacio, valida contra `odoo.LogLevels` → `ErrInvalidLogLevel`, devuelve `rest` sin la flag); los tres `Run*` lo llaman primero y envuelven el argv con `WithLogLevel`. `commandFlags` (install/update/uninstall += `--level`) y `helpSections` (sub-fila `--level <lvl>` en los tres) actualizados. Tests: `odoo/cmd_test.go` (`WithLogLevel` append/no-op) y `modules_test.go` (`extractLevel`: none/space/equals/con-otras-flags/inválido/bare-sin-valor). Build/vet/tests verdes; pendiente prueba real contra Odoo (verbosidad efectiva).
- [x] Unit 32 — recipe-runner. `echo run <file>` ejecuta una receta de comandos Echo (uno por línea) como script de actualización, fail-fast en el primer paso con exit≠0; `--continue-on-error` corre todos y sale ≠0 si alguno falló; la receta puede venir de stdin (`echo run -` o pipe). Comentarios `#` y líneas en blanco se ignoran. Nuevo `internal/repl/recipe.go`: `RunRecipe` arma una `session` headless (reusa `newSession`) y delega en `runRecipeSteps` (lógica pura: recibe `runStep func(name,args) int` y un `log func(level,msg,...logField)`, así es testeable sin docker); `parseRecipeArgs` (path posicional + `--continue-on-error`, `-`/ausente = stdin), `parseRecipeLines` (puro sobre `io.Reader`). Cada paso pasa por `dispatchParsed` → reusa el exit code de Unit 31, y un paso que pediría prompt sin TTY falla closed abortando la corrida. Líneas de progreso/summary vía `sess.runLog` (logger `echo.run`, estilo Odoo). `run` se enruta en `main.go` dentro del bloque one-shot (no está en `Registry`/`dispatchNames` — no es comando del REPL); footer "Scripting (one-shot)" añadido a `runHelp` **fuera** de `helpSections()` para no romper el cross-check `Registry↔help`. Tests `recipe_test.go`: `parseRecipeLines` (comentarios/blancos), `parseRecipeArgs` (tabla), y `runRecipeSteps` con runStep stub (fail-fast salta pasos siguientes, continue-on-error corre todos→1, all-pass→0). Build/vet/tests verdes; smoke-test del binario confirma 0/2/1/0.
- [x] Unit 31 — script-mode. `echo <cmd> [args]` corre un solo comando y sale (bare `echo` sigue abriendo el REPL); generaliza la rama projectless de `connect` en `main.go`. Nuevo `internal/repl/script.go`: `RunOnce` arma una `session` headless vía el `newSession` extraído de `Start` (sin loop de prompt) y llama `dispatchParsed`, devolviendo `session.exitCode`; `IsScriptCommand` filtra los comandos one-shot (todos los de `dispatchNames` salvo `clear`/`exit`/`quit`). Exit codes (`exitOK/Error/Usage/Cancelled` = 0/1/2/3) mapeados por `scriptExitCode(err, errCount)` y fijados en todos los caminos terminales (`finalize`, `readonlyFinalize`, `*FailureLog`, `successLog`, `shellExitLog`, `shellCancelledLog`, `runInit`/`runReset`, default de `dispatchParsed`). Guard de no-interactividad en `internal/cmd/interactive.go` (`ErrNonInteractive`, seam `stdinIsTTY`, `requireTTY(hint)`) aplicado a `runFuzzyPicker`/`runSingleFuzzyPicker`, `confirmProd`/`confirmDrop`/`confirmNeutralize`/`confirmI18nProd`, y a la entrada de `RunInit`/`RunReset` → sin TTY devuelven `ErrNonInteractive` (mapeado a exit 2) en vez de colgarse; los `run*` enrutan `ErrNonInteractive` a `finalize` (línea ERROR limpia, sin auto-copy). Flag `-C/--project-dir` parseado en `main.go` (`extractProjectDir`, estilo `git -C`) antes del `FindRoot`; FindRoot fallido en one-shot → exit 2 (no cae al REPL); comando desconocido → exit 2. Tests: `script_test.go` (`IsScriptCommand`, tabla `scriptExitCode`), `interactive_test.go` (`requireTTY` + todos los call-sites fallando closed con el seam). Build/vet/tests verdes; smoke-test del binario confirma exit 0/1/2 y que el guard no cuelga; pendiente prueba real contra un proyecto Odoo.
- [x] Unit 30 — db-neutralize. Expone la neutralización nativa de Odoo en Echo. Nuevo builder `odoo.Neutralize(c)` → `["odoo","neutralize", <conn flags>]` (subcomando v16+, flag set idéntico en 17/18/19; corre y sale solo, sin `--stop-after-init`). `internal/cmd/db.go`: `dbFlags.neutralize` (+ case `--neutralize` en `parseDBArgs`); nuevo `RunDBNeutralize` (requiere DB+Odoo container, target = posicional → `cfg.DBName` → picker `runSingleFuzzyPicker`; guard `confirmNeutralize` rojo SOLO si `target==cfg.DBName` o `stage=prod`, saltable con `--force`; footer `→ <db> (neutralized)`); helper compartido `neutralizeDB` (arma `odoo.Conn` desde `.env` como `RunOdooShell` y `docker.Exec` sobre el container de Odoo, sin guard de conexiones — neutraliza con Odoo arriba). `RunDBRestore` cablea `--neutralize`: `restoreFromZip` ahora devuelve el sufijo del footer (`" (with filestore)"`/`""`) en vez de imprimir `→`, y el footer se centraliza en `RunDBRestore` añadiendo `" (neutralized)"` tras neutralizar el target recién restaurado (último paso, para que un fallo de neutralize aflore con los datos ya puestos). REPL: `Registry`/`dispatchNames`/`dispatch`/`runDB` switch + `commandFlags` (`db-neutralize: {--force}`, `db-restore` += `--neutralize`) + `helpSections` (Database). Tests: `parseDBArgs` con `--neutralize`, builder `odoo.Neutralize`, y cross-checks `registry_test`/`commandhl_test` sincronizados. Decisiones cerradas en form: ambas superficies + guard solo en DB activa/prod. Build/vet/tests verdes; pendiente prueba real con el contenedor.
- [x] Unit 29 — connect-chrome-window-modes. `connect` dejó de abrir una ventana de Chrome nueva (con perfil temporal `os.MkdirTemp` basura) en cada corrida. Reescrito `connect_cdp.go`: perfil persistente dedicado `connectChromeProfile` (`~/.local/share/echo/connect-chrome`, override `$ECHO_CONNECT_CHROME_PROFILE`, separado del Chrome diario del usuario); `ensureChrome` reusa la instancia viva (lee `DevToolsActivePort` + sondea `/json/version`) o lanza una nueva (`launchPersistentChrome`, borra el port file stale antes). CDP ahora a **nivel browser** (`dialBrowserCDP` vía `/json/version` → `webSocketDebuggerUrl`); `cdpClient.call` toma `sessionID` flatten (`Target.attachToTarget{flatten:true}`). `createWorkingTarget(incognito)`: default `Target.createTarget` (pestaña nueva, contexto default, cookie compartida en el perfil); `--new-window` `Target.createBrowserContext{disposeOnDetach:false}` + `createTarget{browserContextId,newWindow:true}` (ventana incógnito aislada → varios usuarios simultáneos). El target arranca en `about:blank`, se setea la cookie (`setSessionCookieOn`) y luego navega a `<base>/odoo`; en launch fresco se cierra el `about:blank` que Chrome popea (`blankPageTargets`+`closeTarget`). Motivo de descartar "usar el Chrome normal": `session_id` es HttpOnly → solo se inyecta por CDP → requiere `--remote-debugging-port`, inviable en el navegador diario. Flag plumbing: `parseConnectArgs` devuelve `newWindow`, `RunConnect` lo pasa a ambas rutas de `landSessionCookie` + log `window=tab|incognito`; `parseDirectArgs` ahora pasa `--new-window` **y** `--fresh` (este último faltaba en modo directo); `commandFlags["connect"]` y help con `--new-window`. Test de `parseConnectArgs` extendido. Build/vet/tests verdes; pendiente prueba real. Decisiones en form: default=reusar pestaña, `--new-window`=incógnito, perfil dedicado.
- [x] Unit 28 — connect-session-cache. `connect` deja de re-consultar usuarios y re-mintear en cada corrida. Nuevo `internal/config/connect_session.go` (archivo aparte para no tocar `config.go` que otro agente edita): `ConnectSession`, `LoadConnectSessions`/`SaveConnectSession` (best-effort, upsert por login, `writeAtomic`) en `~/.config/echo/connect-sessions/<key>.toml`. Nuevo `internal/cmd/connect_cache.go`: `connectCacheKey` (identidad local `local:<root>:<db>` o remota `ssh:<host>:<path>:<db>` → `config.ProjectKey`), `connectSessionExpired` (TTL 5 días < GC 7 días de Odoo), `probeSession` (GET `<base>/odoo` con cookie, sin seguir redirects: 2xx=válida, 303/error=re-mint), `pickRecentSessions` (picker de logins recientes + sentinel `↻ Fetch all users…`). `connect.go`: `ConnectResult.Reused`, `parseConnectArgs` devuelve `fresh` (`--fresh`), `RunConnect` reestructurado con `connectSelection`/`resolveConnectSelection` (cache hit en `connect <login>` reusa uid sin query; interactivo con cache ofrece recientes; fetch-all cae a la query original), fast-path reusa cookie tras probe o re-minta+guarda. `repl.go` loguea `Session reused (cached)` vs `Session minted`; `commandFlags["connect"]` y help suman `--fresh`. Además **logging estilo Odoo paso a paso** (a pedido del usuario: el comando hacía cosas en silencio y las pocas líneas que tenía no se parecían al resto del CLI): nuevo `ConnectOpts.Log ConnectLogger` que `RunConnect` usa para emitir eventos (`target resolved`, `querying users`/`N user(s) found`, `cache hit`/`validating`/`valid — reusing`/`re-minting`, `minting session`/`session minted`, `opening chrome`) que el REPL renderiza vía `emitOdooLog` con logger `echo.connect[.cache|.mint]`; resumen final `session minted|reused (cached) … mfa=bypassed` + `connect completed`. `ConnectResult.DBName` para mostrar la db correcta en remoto. Reemplazadas las 3 líneas `info` planas. Tests: `parseConnectArgs`, `connectCacheKey` (distinto local/remoto/db, determinista), `connectSessionExpired`, round-trip/upsert de la cache. Build/vet/tests verdes; pendiente prueba real con Odoo. Implementada en worktree aislado (`feat/unit-28-connect-session-cache`) en paralelo a Unit 26 (otro agente).
- [x] Unit 26 — addons-paths-from-conf. Resuelve el `no modules found` reportado por el usuario en una instancia (`habitta_prod`) cuyos addons viven dentro del contenedor y se declaran en `addons_path` de `/etc/odoo/odoo.conf`, no como subcarpetas del host. Dos modos por proyecto (`addons_mode`: `host` default / `conf`). Nuevo `resolveModules(ctx, opts)` reemplaza las llamadas directas a `listAvailableModules` en `pickModulesInteractive` (ahora con `ctx`) y `RunModules`: en modo conf re-lee el conf en vivo (`readContainerFile` = `docker.Exec cat`), parsea (`parseAddonsPath`, puro+testeado, split `=`/`,`, ignora `#`/`;`), y lista dentro del contenedor (`listModulesInContainer`, un `sh -c` con `for d; for __manifest__.py; basename`); refresca rutas guardadas si cambiaron. En modo host escanea como antes; si da 0 → **fallback automático** al conf, y si encuentra módulos cambia a modo conf y lo persiste (`SaveProject`) + emite línea informativa. `modules --config` ahora fija `addons_mode=host` (escape hatch). Config: `AddonsMode`+`ConfPath` (default `/etc/odoo/odoo.conf`) en `Config`/`projectFile`/`Load`/`SaveProject`/`Defaults`/`applyDefaults`. Tests de tabla en `modules_test.go` (`parseAddonsPath` 9 casos, `equalStrings`). Decisiones en form: trigger=fallback automático, fuente=dentro del contenedor, persistencia=rutas+modo (coexiste), formato conf=una línea con comas. Dos ajustes pedidos por el usuario al cierre: (1) `parseAddonsPath` ignora por defecto las entradas cuyo basename **empieza con** `enterprise` (case-insensitive, tolera `/` final — ej. `enterprise`, `enterprise-addons`); a futuro habrá un modo para actualizar cualquier módulo Odoo incl. Enterprise; (2) el `fuzzyPicker` compartido ahora hace scroll — viewport por altura de terminal (`offset`+`height` vía `WindowSizeMsg`, `maxRows=height-chrome`, default 15), `clampScroll` mantiene el cursor visible, `pgup`/`pgdn` paginan, hints `↑/↓ N more` — arregla que listas largas (catálogo completo) desbordaran la pantalla; beneficia a todos los pickers. Build/vet/tests verdes; pendiente prueba real contra la instancia.
- [x] Unit 25 — filestore-in-container. Arregla el bug (reportado por el usuario: `FileNotFoundError` en cada adjunto tras restaurar) de que el filestore se manejaba en el host (`~/.local/share/Odoo/filestore`, ruta de Odoo nativo) cuando Odoo corre en contenedor y lo lee en `/var/lib/odoo/filestore/<db>`. Nuevo config per-proyecto `filestore_path` (default `/var/lib/odoo/filestore`). Restore: `copyFilestoreToContainer` hace `mkdir -p` + `docker cp <src>/. <id>:<dst>` + `chown` best-effort (vía nuevo `docker.ExecAsRoot` = `docker exec -u 0`, matchea el owner de la base con `stat -c %u:%g`). Backup: `pullContainerFilestore` hace `test -d` en el contenedor y `docker cp` hacia afuera a un tmp, que se zipea como `filestore/<db>`. Eliminados los helpers muertos `odooFilestorePath`/`dirExists`/`copyDir` (eran del filestore en host). Build/vet/tests verdes (default de config testeado); pendiente prueba real con el contenedor.
- [x] Unit 24 — flag-highlight-complete. Extiende Unit 21 a las flags. Nuevo registro `commandFlags` (`internal/repl/commands.go`, `map[comando][]flag` solo con flags de usuario; init-guard valida que las keys sean comandos del `Registry`). `commandhl.go` gana `flagState`/`classifyFlag` (ignora `=valor`), `flagStyle` (conocida → `palette.Accent` bold, desconocida → `Faint`, **nunca rojo** para no marcar mal el passthrough de docker/connect), `flagsWithPrefix`, y `lineStyles` (estilo por rune para todo el buffer: token 0 = comando, tokens `-` posteriores = flag). `lineinput.go`: `View()` usa `lineStyles` (cursor del textinput spliceado, blink intacto); `handleTab` refactorizado con helper `tabComplete(matches, prefix, makeBuf)` compartido — completa comando en el primer token, o flags cuando el último token empieza con `-` y hay comando (makeBuf swapea solo el último token). Tests en `commandhl_test.go` (`classifyFlag`, `flagsWithPrefix`, guard `commandFlags↔Registry`). Build/vet/tests verdes; pendiente verificación visual en TTY.
- [x] Unit 23 — db-drop-force-connections. `--force` en `db-drop` ahora termina las conexiones activas y dropea (antes el guard `assertNoActiveConns` abortaba incluso con `--force`, obligando a `down odoo` a mano — caso de la DB huérfana que dejó un restore fallido). Nuevo `docker.TerminateConnections` (`SELECT pg_terminate_backend(pid) … WHERE datname=… AND pid<>pg_backend_pid()`, vía `psqlExec` sobre `postgres`). `RunDBDrop` reestructurado: `--force` → terminate+drop sin confirm; sin `--force` → guard (mensaje ahora apunta a `--force`) + confirm rojo + drop. `RunDBRestore` (rama exists+force) cambia `assertNoActiveConns` por `TerminateConnections` antes del drop. `ErrActiveConns` y help (`db-drop`/`db-restore --force`) actualizados. Decisiones cerradas en form: `--force` termina (no siempre tras confirm) + aplicar también a restore. Build/vet/tests verdes; pendiente prueba real.
- [x] Unit 22 — odoo-native-restore. `db-restore` ahora acepta el `.zip` nativo de Odoo (gestor de BD/odoo.sh) además del de Echo. `restoreFromZip` (`internal/cmd/db.go`) detecta el dump del archivo: `dump.backup` (pg custom) → `docker.Restore`/pg_restore, `dump.sql` (SQL plano) → nuevo `docker.RestoreSQL` (`psql -q -U <user> -d <db>` por stdin, sin ON_ERROR_STOP, matchea `psql < dump.sql`). `findFilestoreInDir` reescrito para distinguir layouts: si todas las entradas bajo `filestore/` son dirs hex de 2 chars (`isHexPrefix`) → layout Odoo, copia `filestore/`; si hay un único subdir con nombre de db → layout Echo, copia ese subdir; destino `~/.local/share/Odoo/filestore/<target>/`. `dbNameFromBackup` ahora recorta el timestamp de Odoo `_YYYY-MM-DD_HH-MM-SS` (regex `odooBackupTS`) además del de Echo `_YYYYMMDD-HHMMSS`. Helpers `fileExists`/`isHexPrefix`. Tests de tabla en `db_test.go` (primer test del paquete `cmd`). Build/vet/tests verdes; pendiente prueba end-to-end con un zip real de Odoo.
- [x] Unit 21 — command-highlight. Resaltado en vivo del primer token del REPL estilo fish: verde/bold si es comando exacto, rojo si ya no puede serlo, color normal mientras es prefijo válido (`ins` → `install`). Solo el comando se recolorea; los args quedan default. Nuevo `internal/repl/commandhl.go` (`classifyCommand` con 3 estados sobre `commandSet` = `Registry` + `exit`/`quit`, `hasCommandPrefix`, `firstToken`, `commandStyle`). `lineModel.View()` ahora rinde la línea a mano (antes `m.input.View()`): colorea el token y splicea el cursor del `textinput` (conserva blink); como Echo no setea `Width`, no hay scroll horizontal que reproducir. `lineModel`/`newLineModel`/`readLine` reciben `theme.Palette` (cableado desde `Start`). Colores de `palette.Success`/`Error` (4 temas). Tests de tabla en `commandhl_test.go` (incluye guard Registry↔commandSet). Build/vet/tests verdes; verificada funcionando por el usuario.
- [x] Unit 02 — `internal/config/` package: `Load`, `SaveGlobal`, `SaveProject`; `~/.config/echo/` layout; `PaletteByName`/`StageFromString` in theme; wired into `main.go` and `repl.go`
- [x] Unit 03 — `init` command (v2): form `huh` 3 steps con iconos nerd-font, project root walk-up, auto-detect compose flavor (docker compose vs docker-compose) persistido en global.toml, `compose ps`/`psql -lqt` para listar containers/DBs, parser `.env` para POSTGRES_USER/DB, charm/log para fatales
- [x] Unit 12 — i18n-commands. Nuevos builders `odoo.ExportI18n` / `odoo.UpdateI18n`; nuevos helpers `docker.ContainerID` / `docker.CopyFromContainer` / `docker.CopyToContainer` (vía binario `docker` directo, no compose). `internal/cmd/i18n.go` con `RunI18nExport`/`RunI18nUpdate`: export usa `/tmp/echo-i18n-*.po` dentro del container y `docker cp` al destino host (default `<addons>/<mod>/i18n/<lang>.po`, `MkdirAll` del dir; override con `--out`); update busca el `.po` en el módulo, `docker cp` al container, `--i18n-overwrite`. Defer de cleanup en `/tmp` siempre. `--out` solo en export. Confirm rojo en prod para update con `--force` bypass. Picker single-select reusa `runSingleFuzzyPicker`. Registry, dispatch, help y tests cross-check sincronizados. Decisiones cerradas via mockup: `--out` flag + tmp+cp pattern, update=import con `--i18n-overwrite`, lang default `es_MX`, un módulo por invocación, picker si falta, crear `i18n/`, prod confirm.
- [x] Unit 13 — history-autocomplete. ↑/↓ history ring (persistencia en `~/.config/echo/history`, dedupe consecutivo, cap 1000) + Tab autocomplete bash-style sobre `Registry` (`internal/repl/commands.go`): 0 matches → no-op, 1 → completa con espacio, >1 → LCP y segundo Tab consecutivo lista debajo del prompt via `tea.Println`. Sólo primer token; case-sensitive prefix. `dispatchNames` y `helpSections`/`helpCommandNames` sincronizados con `Registry` vía tests (`registry_test.go`).
- [x] Unit 04 — docker commands (`up`, `down`, `restart`, `ps`, `logs`). `logs` sigue por defecto (Ctrl+C corta), defaultea al container de Odoo, tail por defecto de 100, soporte para `--copy` al clipboard, `--all`, `-t N`, `--no-follow`. Comando `help` con secciones; `ls` eliminado.
- [x] Unit 05 — module commands (`install`, `update`, `uninstall`, `modules`). Builder en `internal/odoo/`, ejecuta via `compose exec -T <odoo>` y stream al REPL. `install` con `--with-demo`, `update --all`, `modules` escanea `./`, `./addons/`, `./custom/` uno-deep.
- [x] Unit 08 — log-level coloring. `internal/repl/loglevel.go` clasifica líneas Odoo (DEBUG/INFO/WARNING/ERROR/CRITICAL) al kind del tema; `logColorer` mantiene el último kind para que los tracebacks indentados hereden el color. Cableado en `runDocker` y `runModules`.
- [x] Unit 06 — fuzzy picker. `internal/cmd/picker.go` con modelo Bubble Tea fzf-style (filtro siempre activo, Tab toggle, Enter confirma, Esc cancela). `pickModulesInteractive` migrado de `huh.MultiSelect` a `runFuzzyPicker`.
- [x] Unit 07 — action result. `runStats` cuenta líneas ERROR/CRITICAL durante el stream y `finalize` imprime la línea ✓/✗ (con separador en blanco) tras `install`/`update`/`uninstall`/`up`/`down`/`restart`. Cancelación de picker → warn, no ✗. `modulesSummary` filtra flags y formatea `--all` como "all modules".
- [x] Unit 09 — db-commands (`db-backup`, `db-restore`, `db-drop`, `db-list`). Helpers en `internal/docker/postgres.go` (List detallado con tamaño/fecha, ActiveConnections, DatabaseExists, Create/DropDatabase) y `pgdump.go` (Dump/Restore via `compose exec -T`). `internal/cmd/db.go` orquesta: backup a `./backups/` (`.dump` o `.zip` con filestore), restore con picker single-select sobre los backups, drop con `huh.Confirm` rojo, list con `●` marcando la DB activa. Picker reusa `runSingleFuzzyPicker` (variante añadida). Append automático de `backups/` a `.gitignore`. Connection-guard aborta destructivos si hay conexiones activas.
- [x] Unit 10 — shell-commands (`bash`, `psql`, `shell`). Nuevo `internal/docker/shell.go` con `ExecInteractive` (TTY pass-through, SIGINT a la subprocess). `internal/cmd/shell.go` con `RunBash`/`RunPsql`/`RunOdooShell`; `shell` usa flags explícitos `--db_host/port/user/password --no-http` para bypass del entrypoint. Confirmación `huh.Confirm` roja en `prod` para los tres, `--force` la salta.
- [x] Unit 17 — cli-prompt-odoo-info. Prompt REPL Odoo-aware con segmentos configurables (`name`/`version_db`/`stage`/`health`) en `[prompt]` de `global.toml` (`segments`, `name_max`, `health_ttl`). Resuelve compose project name desde `COMPOSE_PROJECT_NAME` > `compose_project` (nuevo per-project) > `filepath.Base(ProjectPath)` normalizado (lowercase + `[a-z0-9]` + `_`/`-` colapsados); right-cut con `…` y clamp `<4`. `healthCache` TTL (default 5s) con `docker inspect -f {{.State.Status}}` (timeout 500ms, hook `dockerInspectFn` para tests), invalidación tras `up`/`down`/`restart`. Glyphs Nerd Font `` (nf-md-docker, Odoo) + `` (nf-md-postgresql, DB) coloreados por estado (running=success, restarting=warning, stopped=error, unknown=faint). Bracket neutro (`dim`) — el chip de stage es ahora el único portador del color de stage. Prompt ya no rinde el `<project>-<id>`. Build, vet y tests verdes.
- [x] Unit 20 — docker-container-log-style. Nuevo `internal/repl/dockerlog.go` con `parseComposeProgress` (regex `Container|Network|Volume|Image <name> <State>`) + `composeStateLevel` (terminal→INFO, transitorios→DEBUG, Error→ERROR, Warning→WARNING). El callback `StreamOut` de `runDocker` re-emite las líneas de compose vía `emitOdooLog` con logger `docker.<resource>`, el verbo como mensaje y `name=<resource>`; resto cae al classify/print existente. `runStats.wrap` cuenta también Error/Warning de compose. Tabla de tests en `dockerlog_test.go`. Cierra el hueco que Unit 08 dejó diferido. Build + tests verdes.
- [x] Unit 11 — test-command. Nuevo builder `odoo.Test(c, TestOpts{Modules, Tags, Update})` que por defecto corre contra el módulo ya instalado (sin `-u`): arma `--test-tags /<mod1>,/<mod2>` automático, agrega `-u` sólo si `Update=true`, y siempre emite `--no-http --http-port=8189` (defensa contra Odoo 19 Enterprise que ignora `--no-http` y choca con el 8069 del Odoo vivo) más `--log-level=test`. `internal/cmd/modules.go` agrega `RunTest`: parsea `--tags X`/`--tags=X` y `--update`, abre picker single-select cuando faltan módulos (`"Modules to test"`), devuelve `[]string` resuelto para que el REPL arme el logger jerárquico `echo.test.module.<mod>`. `repl.go` extiende `dispatch`/`dispatchNames`/`runModules` switch y agrega entradas en `helpSections` (Modules) con `--update` y `--tags`. `Registry` en `commands.go` sincronizado. Flags Odoo CLI verificadas idénticas en v17/v18/v19 (docs oficiales). Build, vet y tests cross-check verdes; no probado contra Odoo real todavía.
- [x] Unit 16 — copy-output. Nuevo `lastOutputBuffer` por sesión (`internal/repl/lastoutput.go`) que captura cada `Line` impresa; `dispatch` lo resetea al inicio de cada comando excepto `copy-last`/`help`/`clear`. Comando `copy-last` (`internal/repl/copylast.go`) copia todo por default, `--errors` filtra a kinds `err`/`warn`. Auto-copy en fallo de `install`/`update`/`uninstall`: cuando `err != nil` o `stats.errors > 0`, `copyFailureLog` empaqueta header `✗ <summary> failed` + líneas err/warn al portapapeles y emite una línea charm/log (`ERRO update failed module=sale copied=true`) vía `*log.Logger` por sesión con `buildLogStyles(palette)`. `internal/clipboard` ahora invierte la prioridad cuando `$SSH_TTY`/`$SSH_CONNECTION`/`$TMUX` están seteadas (OSC 52 primero, helpers nativos después); test de `isRemote` en tabla con `t.Setenv`. `Registry`, `dispatchNames` y `helpSections` extendidos con `copy-last` (sección Shell).
- [x] Unit 18 — connect-command (CDP). Verificado funcionando por el usuario; release 0.5.0. `connect [<login>] [--all] [--force]` abre Chrome ya logueado como cualquier usuario, sin password, sin abrir puertos y sin instalar nada en Odoo. `internal/cmd/connect.go` orquesta: `parseConnectArgs` (login posicional, `--all`), `listConnectUsers`/`mintConnectSession` corren dos scripts Python embebidos (`connect_{list_users,mint}.py`) — local vía `docker compose exec -T … python3 -` o remoto vía `ssh -o BatchMode=yes <host> 'cd <remote_path> && <compose> exec -T … python3 -'` según `[connect].ssh_host`; el mint usa `root.session_store.new()` + `_compute_session_token`. En remoto el mapping (compose/odoo_container/db_container/db_name/stage) **se hereda del perfil de Echo del server**: `resolveConnectTarget`/`fetchRemoteProfile` leen por SSH `~/.config/echo/global.toml` + `~/.config/echo/projects/<sha256(remote_path)>.toml` y los decodifican con `config.ParseRemoteProfile` (nuevos `config.ProjectKey`/`RemoteProfile` exportados) — en local solo se configura `ssh_host`+`remote_path`. DB creds: local desde `.env`, remoto leyendo el `.env` remoto por SSH (`env.Parse` nuevo). `pickConnectUser` reusa `runSingleFuzzyPicker`. La cookie se aterriza **siempre local** en `internal/cmd/connect_cdp.go`: lanza Chrome con perfil temporal + `--remote-debugging-port=0`, lee el puerto de `DevToolsActivePort`, abre WebSocket CDP (`github.com/coder/websocket`, dep nueva) y emite `Network.enable`/`Network.setCookie` (HttpOnly, `secure` según esquema, dominio del `web.base.url`)/`Page.navigate` a `<base>/odoo`. Config: nueva sección per-proyecto `[connect]` (`ssh_host`/`remote_path`/`chrome_path`). REPL `runConnect` usa `startLog`/`finalize`/`connectFailureLog`. Eliminados el helper-server `:8003` y el modo `--tunnel` (`connect_helper.go`, `docker/inspect.go`). Build/vet/tests verdes (incluye round-trip de config); **verificado funcionando por el usuario** y publicado en el release 0.5.0.

## Open Questions

1. What ASCII logo should appear in the header by default? (`echo`, `planet`, `python`, `anchor`)

## Resolved Questions

- ~~_Should history persist between sessions (file-based) or stay in-memory only for v1?_~~ — resolved 2026-05-11/13 with Unit 13: file-based persistence at `~/.config/echo/history`, cap 1000, dedupe consecutivo.
- ~~_Exact Odoo CLI flag differences between v17, v18, v19 for `install`/`update`/`test`._~~ — resolved 2026-05-25 with Unit 11: docs oficiales confirman flag set idéntico en las tres versiones (`--test-enable`, `--test-tags <spec>`, `-i`/`-u` y `--stop-after-init`). No requiere version branching.

## Architecture Decisions

- **connect impersonation = forge a session, never a password.** El mint
  reproduce `Session.finalize` (`root.session_store.new()` +
  `_compute_session_token`), no autentica con credenciales. 2FA queda
  bypasseado a propósito (uso dev); el único freno es el prod-confirm.
- **Cookie por CDP, no por bookmarklet ni helper-server.** La `session_id`
  de Odoo es HttpOnly: JS no puede sobreescribirla y un helper-server
  exigía abrir un puerto inbound (descartado). Chrome DevTools Protocol
  (`Network.setCookie`) la setea a nivel de red → cero puertos, cero
  módulo en Odoo.
- **Echo (para connect) corre en el laptop; el server solo se toca por
  SSH para mintear.** El navegador siempre es local, así que la cookie se
  aterriza local; lo único remoto es ejecutar el script Python en el
  contenedor.
- **El remoto hereda el perfil de Echo del server, no se re-declara en
  local.** `fetchRemoteProfile` lee por SSH `global.toml` +
  `projects/<sha256(remote_path)>.toml` (mismo hash que usa Echo local).
  Requirió persistir `project_path` en el perfil + auto-migración en el
  arranque (`BackfillProjectPath`).
- **Targets directos basados en `~/.ssh/config`.** Echo no gestiona SSH:
  referencia el alias del Host; el modo directo `echo connect <name>`
  guarda targets nombrados en `global.toml` y descubre proyectos leyendo
  la config de Echo del server (nunca escaneando dockers).
- **La versión del binario siempre muestra el commit.** `+<shortsha>`
  siempre, `.dirty` cuando el árbol está sucio (ldflags del Makefile), para
  poder identificar exactamente qué build corre — no solo cuando hay
  cambios sin commitear.

## Session Notes

- 2026-06-24: Unit 72 (remote-restart-logs) a pedido del usuario ("¿podemos tener remote restart y logs?"). `restart` y `logs` ahora aceptan `--from <target>`/`--remote` y corren el verbo de compose sobre SSH reutilizando el transporte de deploy/shell (`resolveRemoteShell` → `resolveRemoteTarget`+`fetchRemoteProfile`, `remoteComposeCmd`, `runSSHStream`), sin SSH nuevo. Decisiones cerradas en form: (1) `logs` remoto mantiene follow por defecto (stream largo sobre SSH, sin TTY); (2) `restart` remoto sin servicio reinicia el contenedor Odoo del perfil remoto (servicios explícitos acotan); (3) `restart` remoto confirma cuando el stage REMOTO es prod vía `confirmRemoteProd` (`--force` salta) — más estricto que el restart local, que no confirma. Implementación: `RunRestart`/`RunLogs` branchean por `remoteFlagsIn`; nuevo `internal/cmd/docker_remote.go` (`runRemoteRestart`/`runRemoteLogs`/`runRemoteLogsAndCopy`/`remoteServiceArgs`); `parseLogsArgs` extraído de `RunLogs` (compartido local/remoto, consume `--from`/`--remote`); `DockerOpts` gana `Log` (progress nil-safe). REPL: `runDocker` pasa `Log: sess.cmdOdooLogger(name)` y solo invalida el health local en restart NO-remoto (`remoteRunFlags`). `commandFlags` suma `restart`+extiende `logs` con `--from`/`--remote`; help con subflags; `main.go` `projectlessOneShot` incluye `restart`/`logs` solo con flag remoto. Tests `docker_remote_test.go` (remoteServiceArgs/parseLogsArgs/ensamblado del comando) + ajuste de `build_test.go` (logs gana `--from`/`--remote`). Build/vet/test verdes; verificación EN VIVO contra un host remoto pendiente del usuario (sin SSH en el entorno dev). Spec `72-remote-restart-logs.md` + fila build plan (deps 60, 62). Falta commit (vía commitcraft).
- 2026-06-09: Pulido de formato a pedido del usuario (cierre de sesión). (1) El recap de `echo run` ahora lleva `step` y `status` como campos (`step=1/4 status=ok cmd=stop took=…`, msg vacío); nuevo `valueStyleFor(key,value)` en `emitOdooLog` colorea el valor de `status` por desenlace (ok verde/failed rojo/cancelled·skipped ámbar) y el de `cmd` por su acción (paleta del logger); `emitOdooLog`/`plainOdooLog` omiten el segmento msg si está vacío (sin doble espacio). (2) `report --copy` pasó de dos líneas (header + confirmación plana) a UNA línea Odoo (`echo.report: copied N lines to clipboard …`); no-match y copy-fail también una línea WARNING/ERROR. Tests de recap migrados a buscar por campo `step`/`status` (`findRecap`). Build/vet/tests verdes + smoke con CLICOLOR_FORCE confirma status verde y cmd coloreado. Commit `[IMP]`. (La línea de progreso `step N/M → cmd` se dejó como está.)
- 2026-06-09: Unit 41 (recipe-step-silent) a pedido del usuario: poder silenciar ciertos logs en `run` (ej. `update --silent`), en pantalla y en el archivo. Confirmado al usuario que aplica a cualquier comando no-interactivo (stop/up/etc.) porque se intercepta en el runner. Spec `41-recipe-step-silent.md` + fila build plan. Decisiones en form: ambos modos (`--silent` total + `--silent=<lvl>` por umbral, deja pasar lo más grave); capturar igual para `report`. Implementación: gate por nivel en sess.print + emitOdooLog vía var de paquete `suppressLevel`, runner la togglea por paso. Build/vet/tests verdes + smoke. Commit inmediato.
- 2026-06-09: Mejora a Unit 34 (`--log`) a pedido del usuario: `echo run <receta> --log=<dir>` (p.ej. `--log=.`) ahora escribe un `<receta>.log` con el nombre de la receta dentro de ese directorio (antes `--log=.` fallaba porque trataba `.` como ruta de archivo). Nuevo `resolveLogDest(dest, recipePath)` puro/testeable que detecta si el valor es un directorio existente y arma `<dir>/<recipeLabel>.log`; `openRunLog` lo llama primero. Help y spec 34 actualizados. Confirmado al usuario que NO afecta a `report` (lee `last-run.json`, no el transcript). Build/vet/tests verdes + smoke (`--log=.` → `deploy.log` en cwd; `--log=logs` → `logs/deploy.log`). Commit `[IMP]` propio.
- 2026-06-09: Unit 40 (run-report) a pedido del usuario: poder hacer `echo report --step=1 --copy --level=warn` tras un run, copiando logs por nivel. Discusión de viabilidad (el reto: report es otro proceso sin memoria → persistir el run). Spec `40-run-report.md` + fila en build plan. Decisiones en form: `--level` exacto + `--min-level` umbral (con error/critical válidos); persistir siempre `last-run.json`; one-shot + REPL. Reusa lastOutputBuffer (captura por paso), clipboard, regexes de nivel. Dos detalles afinados durante el build: fallback al Kind para líneas sin token (si no, los warns propios de Echo se perdían) y reset del buffer por paso (los meta no resetean → doble conteo, detectado en smoke). Build/vet/tests verdes + smoke end-to-end. Commit inmediato.
- 2026-06-09: Unit 39 (recipe-pick-file) a pedido del usuario: `echo run --pick` muestra un selector de archivos `.echo` del directorio donde se ejecuta. Spec `39-recipe-pick-file.md` + fila en build plan. Decisiones en form: flag `--pick` (no `--file`); búsqueda solo en el directorio actual (sin recursión). Nuevo export `cmd.PickOne` para reusar el single-picker desde repl; `echoRecipesIn` puro/testeable. Build/vet/tests verdes + smoke. Commiteado de inmediato (lección de no apilar).
- 2026-06-09: Retoque a Unit 37 a pedido del usuario: la línea de totales `run summary` ahora siempre incluye `errors` y `warnings` (antes warnings era condicional a >0 y errors no aparecía). `runRecipeSteps` acumula `errTot` además de `warnTot` y ambos van siempre en los totFields. Tests de recap actualizados (`errors`/`warnings` asertados, incl. `errors=0 warnings=0` en all-pass). Build/vet/tests verdes + smoke-test. Commit propio sobre las Units 35-38 ya commiteadas.
- 2026-06-09: Unit 38 (module-start-line-resolved) a pedido del usuario: tras un `update --last` no sabía qué módulos corrió porque la línea de inicio salía genérica (se emitía antes de resolver picker/`--last`). Spec `38-module-start-line-resolved.md` + fila en build plan. Diseño: callback `OnResolve` en la capa cmd que dispara justo antes de `runOdoo`; el REPL emite ahí la línea de inicio (`startResolved`) con logger por módulo + campo `modules=`. Limpieza de `resolvedFromArgs`/`startLogger`. Build/vet/tests verdes. Pendiente: commit (Units 35/36/37/38, cada una su commit) + posible rename de la rama (ya no es solo "update-last-recall") + merge a main.
- 2026-06-09: Unit 37 (recipe-run-summary) a pedido del usuario: que `echo run <file>` agregue una salida final con, por paso, si se completó/warnings/etc. ("lo que veas útil"). Spec `37-recipe-run-summary.md` + fila en build plan. Diseño: surfacing de los contadores `runStats` de cada comando en la sesión (`lastErrors`/`lastWarnings`, como `exitCode`), `stepOutcome` con duración medida en el closure, y recap+totales en `runRecipeSteps` (que sigue puro/testeable). Build/vet/tests verdes + smoke-test. Pendiente: commit (Units 35/36/37, cada una su commit atómico) + merge de la rama a main.
- 2026-06-09: Unit 36 (loose-severity-log-fallback) a pedido del usuario, que notó que `Warn: Can't find .pfb for face 'Courier'` (stderr de wkhtmltopdf) salía sin estilo porque no matchea el formato Odoo/loguru. Spec `36-loose-severity-log-fallback.md` + fila en build plan. Decisiones en form: reformatear a estilo Odoo (vs. solo colorear vs. ocultar) y que solo WARNING cuente al resumen. Implementado siguiendo el patrón de `parseComposeProgress` (Unit 20): nuevo `looselog.go` + `emitStreamLine` compartido + conteo solo-warning. Build/vet/tests verdes. Pendiente: commit (vía commitcraft) junto/después de Unit 35, y luego el merge de la rama a main que pidió el usuario.
- 2026-06-09: Unit 35 (update-last-recall) planeada e implementada a pedido del usuario ("a veces necesito correr el update en el mismo módulo"): `update --last` + resaltado de los últimos módulos en el picker + confirmación de "repetir último" al confirmar el picker sin selección. Spec `35-update-last-recall.md` + fila en build plan. Cuatro decisiones cerradas en form: alcance solo `update`; persistir en disco (sobrevive reinicio); `--last` repite `--all`; confirmar solo sin `--last`. Aclaración del usuario en el segundo form replanteó la confirmación: no es un paso extra siempre, sino el fallback del **picker vacío** (selección vacía = "usa el último"), uniendo las dos features. Implementación: `internal/config/last_update.go` (persistencia best-effort calcada de connect-session), rework de `RunUpdate`, `runFuzzyPickerCore` con flag `canceled`, `ModulesOpts.Interactive`, `sess.interactive=true` solo en `Start`. Build/vet/tests verdes (nuevos tests de config y picker). Pendiente: prueba real contra Odoo y commit (vía commitcraft).
- 2026-06-09: Release v0.6.0 publicado (bump 0.5.0→0.6.0 + promoción del CHANGELOG en el mismo commit, tag y GH release). Luego se planeó y arrancó el modo script: specs 31 (script-mode) y 32 (recipe-runner) escritos, build plan actualizado. Units 31 (script-mode), 32 (recipe-runner), 33 (module-log-level: `--level`) y 34 (recipe-run-log: `echo run --log`) implementadas en rama `feat/unit-31-script-mode`, cada una en su propio commit (más un `[IMP]` de comentarios inline en recetas). Verificadas con build/vet/tests + smoke-tests. Decisiones del usuario: guard no-interactivo fail-closed por TTY; alcance 31+32; `-C/--project-dir`; comentarios inline en recetas; `--level` en los tres comandos de módulos; log opt-in vía `--log` (transcript completo) en el dir de config. Pendiente: prueba real contra una instancia Odoo (verbosidad de `--level` y transcript de un update real).
- 2026-05-07: Project initialized with spec-driven-dev. Context files generated from
  DESIGN_TOKENS.md and initial conversation. First deliverable: header + `ls` command.
- 2026-05-08: Unit 02 complete. Config package with TOML, atomic writes, defaults, and tests. Theme and stage now come from `~/.config/echo/` instead of being hardcoded.
- 2026-05-08: Unit 03 complete. `init` command with `huh` form, docker-compose auto-detect, and live prompt update on confirm.
- 2026-05-08: Unit 03 reescrito (v2) tras feedback. Eliminado parsing YAML; ahora todo viene de docker live (`compose ps`, `psql -lqt`). Nuevo `internal/project/` (walk-up al root), `internal/docker/` (compose+psql), `internal/env/` (.env parser). Compose flavor (`docker compose` vs `docker-compose`) auto-detectado y persistido en `global.toml`. Iconos nerd-font en form, banner final y prompt. `charmbracelet/log` para fatales.
- 2026-05-11: Historial de comandos (Unit 11 parcial). REPL migrado de `bufio.Reader` a `bubbles/textinput` para soportar ↑/↓. Persistencia en `~/.config/echo/history`, cap 1000 entradas, dedupe consecutivo. Autocomplete pendiente.
- 2026-05-11: Unit 04 completo. Docker commands con streaming (`up`, `down`, `restart`, `ps`) y `logs` con follow por defecto, default Odoo, tail 100, `--copy` al clipboard via atotto/clipboard, `--all`, `--no-follow`. `help` real con secciones; `ls` eliminado. Spec `04-docker-commands.md` escrito.
- 2026-05-12: Unit 05 completo. Investigación de flags Odoo CLI v17/v18/v19 (idénticos para install/update/uninstall/test). Nuevo paquete `internal/odoo/` con builders. Helper `docker.Exec` para `compose exec`. `modules` lista local (one-deep en `./`, `./addons/`, `./custom/`). Spec `05-module-commands.md`.
- 2026-05-12: Fix entrypoint bypass — `odoo.Conn` con flags `--db_host/--db_port/--db_user/--db_password` explícitos. Echo lee POSTGRES_USER/PASSWORD/PORT del `.env` y usa `cfg.DBContainer` como host.
- 2026-05-12: Specs 06 (fuzzy-picker), 07 (action-result), 08 (log-level-coloring) escritos. Build plan reordenado para meterlos antes que db-commands. Sesión cerrada con cwd locked por macOS Full Disk Access; próxima sesión reanuda con build/test del prototipo en `internal/cmd/picker.go`.
- 2026-05-12: Unit 08 implementado. Nuevo `internal/repl/loglevel.go` con regex de niveles Odoo y wrapper `logColorer` con herencia para tracebacks indentados. `runDocker` y `runModules` instancian un `logColorer` fresco por comando y lo aplican al callback de stream. `go build ./...` pasa.
- 2026-05-12: Unit 06 + Unit 07 implementados y commiteados. Unit 06: picker fzf-style en Bubble Tea reemplaza huh.MultiSelect. Unit 07: `runStats` cuenta ERROR/CRITICAL, helper `finalize` imprime ✓/✗ con separador, `modulesSummary` filtra flags. Triple polish 08→07→06 cerrado.
- 2026-05-13: Unit 09 implementado. Nuevos helpers en `internal/docker/postgres.go` y `pgdump.go`; `internal/cmd/db.go` con los cuatro `RunDB*`; picker extendido con variante `runSingleFuzzyPicker`. Backups en `./backups/` con append automático a `.gitignore`. Drop con `huh.Confirm` rojo, connection-guard previo, restore con picker sobre `*.dump`/`*.zip`. Build + vet limpios; no probado contra Postgres real.
- 2026-05-13: Unit 10 implementado. `docker.ExecInteractive` (TTY pass-through estilo `LogsFollow`, sin `-T`), `cmd.RunBash`/`RunPsql`/`RunOdooShell` y dispatch en el REPL. Multi-DB queda fuera por decisión explícita: siempre `cfg.DBName`. Prod confirm con `--force` para saltar. Build + vet limpios.
- 2026-05-13: Unit 12 cerrado. Nuevos `odoo.ExportI18n`/`odoo.UpdateI18n`, `docker.ContainerID`/`Copy{From,To}Container`. `internal/cmd/i18n.go` con flujo tmp-in-container + `docker cp` + defer cleanup. Default lang `es_MX`. `--out` solo aplica a export. Update siempre va a `<addons>/<mod>/i18n/<lang>.po`; falta del archivo → `ErrTranslationMissing`. Prod confirm rojo en update con `--force`. Picker single-select para `<mod>` ausente. `Registry`, `dispatchNames`, `helpSections` extendidos; tests cross-check siguen verdes. Build + vet + test limpios; no probado en TTY con Odoo real.
- 2026-05-13: Unit 13 cerrado. Nuevo `internal/repl/commands.go` con `Registry` ordenado + `matchPrefix`/`longestCommonPrefix`. `lineinput.go` maneja `tab` con latch `lastWasTab`; `tea.Println` imprime la lista de matches sobre el prompt. `lineModel` ahora recibe `infoStyle`. `repl.go`: `dispatchNames` declarado junto a `dispatch`, `runHelp` extraído a `helpSections()` + `helpCommandNames()` helper. `registry_test.go` con cinco tests guarda la consistencia Registry↔help↔dispatch y verifica `matchPrefix`/`longestCommonPrefix`. Decisiones cerradas en mockup: bash UX, prefix case-sensitive, ignore Tab on empty.
- 2026-05-15: Fix `ErrCancelled` — texto cambiado de "init cancelled" a "cancelled by user" porque se reusa en pickers y prod confirms (commit dbbef1d).
- 2026-05-15: Unit 16 cerrado. `lastOutputBuffer` por sesión + `copy-last [--errors]` + auto-copy en fallo de install/update/uninstall con línea `charmbracelet/log` Error usando `*log.Logger` por sesión estilado con palette. `internal/clipboard.WriteAll` ahora detecta SSH/tmux vía `isRemote()` y prioriza OSC 52 en remoto; test de tabla con `t.Setenv`. Decisiones cerradas en mockup: Q1=A (copy-last completo, --errors filtra), Q2=A (solo err/warn), Q3=A (solo modules), Q4=A (línea única vía charm/log), Q5=B (auto-detect SSH/TMUX).
- 2026-05-15: Polish Unit 16 (commit ba4faf8). RunInstall/RunUpdate/RunUninstall devuelven `([]string, error)` con módulos resueltos (sentinel `["--all"]`); runModules los usa para summary/`module=`. classifyOdooLog ahora valida prefijo completo `ts pid LEVEL` (regex anclada) — los frames que contienen la palabra `DEBUG` o `INFO` dejaron de romper la herencia err/warn. `runStats.warnings` cuenta WARNING-prefijadas. `lastOutputBuffer.FromFirstError` captura desde la primera err/warn — clipboard ahora trae traceback completo + INFOs de shutdown.
- 2026-05-15: Render por segmentos Unit 08 (commit 4432316 → c87e659). Nuevo `internal/repl/logrender.go` con `formatOdooLine` (ts dim, pid faint, LEVEL bold coloreado, db, logger, msg) integrado en `sess.print`. Después de iteración el render se extendió: db con `palette.Accent`, logger con rotación 8-pasteles (`loggerPalette` + FNV-1a estable). Warning del tema Charm cambiado de naranja `#f6ad55` a amarillo pastel `#fde047`.
- 2026-05-15: Las líneas finales OK y ERRO ya no usan `charmbracelet/log` — `internal/repl/logemit.go` con `emitOdooLog` arma manualmente la línea estilo Odoo (ts coma-ms, PID via `os.Getpid()`, level chip, db, logger jerárquico, msg, key=val coloreados). Logger `echo.<cmd>.module.<mod>` (1), `.modules` (N), `.all`. `internal/repl/logstyle.go` y field `sess.log` eliminados.
- 2026-05-16: Auto-copy extendido a todos los subprocess commands (commit cfdccc5). `ExecInteractive` ahora envuelve el subproceso en un PTY del host con `github.com/creack/pty` para capturar el stream combinado stdout+stderr (docker exec con `-t` los fusiona en el PTY del contenedor; teear solo stderr capturaba vacío). Stdin pasa a raw mode local, SIGWINCH se propaga vía `pty.InheritSize`. `RunBash`/`RunPsql`/`RunOdooShell` devuelven `(captured, error)`. Nuevos helpers `shellFailureLog` (usa captured) y `commandFailureLog` (usa lastOutputBuffer) en copylast.go. Cobertura: install/update/uninstall, shell/bash/psql, i18n-*, db-backup/restore/drop, docker up/down/restart. Read-only (ps/logs/db-list/modules) quedan fuera.
- 2026-05-18: Header del clipboard cambiado de `✗ <name> failed` a línea Odoo-style plain vía `plainOdooLog`. Nuevo `failureLogger` sufija `.error` al logger del comando — aplicado tanto al header del clipboard como a la línea ERRO del terminal para que ambas estén sincronizadas (commit 53d3404). Loggers de fallo: `echo.shell.error`, `echo.update.module.<mod>.error`, `echo.up.error`, etc.
- 2026-05-18: Fix `RunOdooShell` (commit 957f4a4). `--db_port=` vacío hacía crashear Odoo CLI con `ValueError: int('')`. Ahora se usa `odoo.Conn.Flags()` (expuesto público) que omite flags vacíos, con default defensivo `5432` si `.env` no lo declara.
- 2026-05-18: Línea `$ <name>` reemplazada por log Odoo-style INFO `echo.<cmd>.start` (commit 8045acc). Para module commands el logger embebe el target (`echo.update.module.sale.start`); para otros, los positionals salen como campo `args=`. Shells interactivos también ganan línea INFO de salida limpia (`echo.shell: shell exited`). Triplet por comando: `.start` / base / `.error`.
- 2026-05-18: Unit 17 cerrado. Prompt Odoo-aware con `promptBuilder` por segmentos en `internal/repl/prompt.go`; `composename.go` resuelve compose project name (env > toml > path normalizado, right-cut con `…`); `health.go` cachea `docker inspect` con TTL e invalidación tras up/down/restart. Config extendida con `[prompt]` (segments/name_max/health_ttl) y `compose_project` per-project. Bracket pierde color de stage; chip `stage` aparte. Decisiones cerradas en form: numeración 17 + build plan, compose name al estilo docker-compose, right-cut + ellipsis, glyphs nf-md-docker + nf-md-postgresql.
- 2026-05-19: Polish triple post-Unit 17. (a) `sess.readonlyFinalize` en `internal/repl/copylast.go` para que `ps`/`logs`/`modules`/`db-list` cierren con `INFO echo.<cmd>` o `ERROR echo.<cmd>.error` (sin auto-copy, son read-only) — commit c27ad18. (b) Nuevo comando `stop` (docker.Stop + cmd.RunStop) registrado en Registry/dispatch/help; invalida health del prompt como up/down/restart — commit bbbc3be. (c) `down` ahora exige `huh.Confirm` rojo cuando `stage=prod`; `--force` salta y se filtra antes de pasar a compose; `DockerOpts` gana `Palette` y nuevo helper `maybeConfirmDockerProd`+`stripFlag` paralelo a `shell.go` — commit 15c5582. Decisiones cerradas en form: añadir end log a los 4 read-only, confirm en prod con --force, stop sin confirm.
- 2026-05-19: `finalize()` reescrito a Odoo-style (commit f916ba6). Emite `INFO echo.<cmd>: <name> completed` en éxito, `WARNING echo.<cmd>.cancelled` en cancel y `ERROR echo.<cmd>.error` en errores residuales — reemplaza el legacy `✓ / ✗ <summary>`. Eliminados `modulesSummary`, `i18nSummary`, `dbSummary` y el parámetro `summary` de `finalize`/`copyFailureLog` (los args ya viajan en `.start`). Todos los comandos (`install/update/uninstall`, `up/down/stop/restart`, `i18n-*`, `db-backup/restore/drop`, shells, read-only) comparten ahora el mismo frame start/end.
- 2026-05-19: Release v0.4.0 publicado (commit be62700, tag `v0.4.0` en `Cerebellum-ITM/Echo`). Incluye Unit 17 + polish post-17 + finalize unificado. `make build_release` produjo `darwin_arm64`/`linux_amd64`/`linux_arm64`; `gh release create` subió los tres assets con notas extraídas del CHANGELOG. Versión bump 0.3.1 → 0.4.0 en `internal/repl/repl.go`. También: `.commitcraft.toml` ganó bloque `[release_config]` (commit 3b13728) y remote `origing` renombrado a `origin`.
- 2026-05-18: Ctrl+C en shells interactivos (commit 637781f). Odoo atrapa SIGINT y sale con código 1 + traceback `KeyboardInterrupt` — no podíamos discriminar por exit code. `ExecInteractive` ahora levanta `atomic.Bool` en el signal handler del padre y devuelve `(captured, interrupted, error)`. `runShell` chequea la bandera y emite `shellCancelledLog` (WARN level, logger `echo.<cmd>.cancelled`) en lugar de auto-copy del traceback que el usuario provocó.
- 2026-05-25: Unit 18 (connect) pausada en rama `feat/unit-18-connect-tunnel` (commit WIP `5e8b1bb`). Helper HTTP en :8003 con modo direct (host de `web.base.url`) y `--tunnel` (helper en `localhost`, redirect a `localhost:18069`, IP del container resuelta vía nuevo `docker.ContainerIP`). En tunnel mode el browser no aterriza el hit aunque el server escuche — pendiente verificar SSH forwards + scope de cookie en localhost. Mint script migrado a `root.session_store.new()` y a `parse_config(--db_host/port/user/password)` vía env vars `ECHO_DB_*` (fixed el `Connection to the database failed` al socket Unix por defecto).
- 2026-06-02: Unit 20 cerrado. Las líneas de progreso de `docker compose` (`Container … Restarting/Started`, `Network … Created`, etc.) dejan de pasar crudas y se reformatean al estilo Odoo de Echo. Nuevo `internal/repl/dockerlog.go` (`parseComposeProgress` + `composeStateLevel`); el callback de `runDocker` las emite vía `emitOdooLog` como `docker.<resource>: <verbo> name=<resource>`, con estados terminales en INFO y transitorios en DEBUG (faint), y `Error`/`Warning` mapeados a ERROR/WARNING que alimentan `runStats` para el finalize. `ps`/`logs` y las líneas Odoo reales quedan intactas. `dockerlog_test.go` cubre el parser y descarta logs Odoo/filas de `ps`. Decisión cerrada en form: logger por recurso (Opción A) + nombre como campo `name=`. Cierra el gap diferido explícitamente en Unit 08. Build + `go test ./internal/repl/...` verdes; no probado contra docker real todavía.
- 2026-05-25: Unit 11 cerrado. `odoo.Test(c, modules, tags)` agrega builder paralelo a Install/Update/Uninstall (`--test-enable` o `--test-tags <spec>` cuando hay `--tags`, luego `-u <mod> --stop-after-init`). `cmd.RunTest` parsea `--tags` en ambas formas (`--tags X` y `--tags=X`), reusa `pickModulesInteractive("Modules to test")` cuando no vienen módulos, devuelve `[]string` para que el REPL arme el logger. `repl.go` extiende `dispatchNames`, switch en `dispatch`, switch en `runModules`, y agrega entrada+subflag en `helpSections` (Modules). `commands.go` Registry sincronizado. Flags Odoo CLI verificadas idénticas en v17/v18/v19 (cierra open question del tracker). Build + vet + tests verdes; no probado contra Odoo real todavía.
- 2026-06-08: Unit 18 (connect) reescrita por completo del enfoque pausado (helper-server `:8003` + `--tunnel`, que exigía abrir puerto y no aterrizaba el hit) al **modo CDP**, y cerrada. El WIP `5e8b1bb` (que traía co-autor de IA) se deshizo con soft-reset y se rehízo limpio por CommitCraft. Mint local (`compose exec`) o remoto (`ssh … compose exec`); cookie aterrizada en Chrome local vía Chrome DevTools Protocol (`Network.setCookie` HttpOnly + `Page.navigate`), dep nueva `github.com/coder/websocket`. Eliminados `connect_helper.go` y `docker/inspect.go`. El remoto hereda el perfil de Echo del server por SSH (`ParseRemoteProfile`, `ProjectKey` exportado). **Modo directo sin proyecto** `echo connect [<name>]` (dispatch en `main.go` antes del `FindRoot`): targets nombrados en `global.toml`, registrados eligiendo un Host de `~/.ssh/config` + un proyecto Echo del server (perfiles ahora con `project_path` + `BackfillProjectPath` auto-migra al arrancar). Preferencia HTTPS (`preferHTTPS` sondea el host). Versión del binario ahora muestra el commit siempre (`+<shortsha>`, `.dirty` si sucio). **Verificado funcionando por el usuario.** Mergeado a `main` (`031a9e2`, `--no-ff` con resolución de conflictos vs Units 19/20/11 + version-meta) y **publicado release v0.5.0** (tag + 3 binarios vía `gh release create`). `make build` ahora instala en `~/.local/bin/echo_cli`. Specs: `18-connect-command.md` reescrito al diseño CDP.
- 2026-06-08: Unit 21 (command-highlight) implementada. Resaltado fish-style del primer token en el REPL vía `lineModel.View()` propio + `commandhl.go` (clasificador de 3 estados sobre `Registry`+`exit`/`quit`). El cursor del `textinput` se splicea para conservar blink; sin scroll horizontal porque Echo no usa `Width`. Spec `21-command-highlight.md` + fila en build plan + código + tests + CHANGELOG + tracker, todo en el mismo commit (a pedido del usuario: docs no van separadas del feat). Build/vet/tests verdes; verificada funcionando por el usuario.
- 2026-06-08: Unit 22 (odoo-native-restore) implementada tras reporte del usuario de que un `.zip` de Odoo no restauraba. Diagnóstico (vía `unzip -l`): era un backup nativo de Odoo (`dump.sql` 162MB + `filestore/<XX>/<sha>` directo + `manifest.json`), no el formato de Echo (`dump.backup` + `filestore/<db>/…`). El error inmediato ("database already exists") venía de que `dbNameFromBackup` no recortaba el timestamp de Odoo y el target quedaba con el timestamp. Fix triple: detección dump.backup/dump.sql en `restoreFromZip` (+ `docker.RestoreSQL`), `findFilestoreInDir` con detección de layout (`isHexPrefix`), y `dbNameFromBackup` con regex del timestamp Odoo. Spec `22-odoo-native-restore.md` + build plan + código + tests + CHANGELOG + tracker en un solo commit. Build/vet/tests verdes; falta probar con el zip real.
- 2026-06-08: Unit 23 (db-drop-force-connections) implementada tras reporte del usuario de que la DB huérfana del restore fallido no se podía borrar (guard de conexiones activas obligaba a `down odoo`). `--force` ahora termina las conexiones (`docker.TerminateConnections`) y dropea, en `db-drop` y en el reemplazo de `db-restore --force`. Sin `--force` el guard sigue protegiendo la DB viva (mensaje apunta a `--force`). Decisión consultada en form: `--force` como trigger + aplicar a restore. Spec `23-db-drop-force-connections.md` + build plan + código + CHANGELOG + tracker en un commit.
- 2026-06-08: Unit 24 (flag-highlight-complete) implementada a pedido del usuario (similar a Unit 21 pero para flags + autocomplete de flags). Decisiones en form: resaltado validado **sin rojo** (conocida=acento, desconocida=tenue) por el passthrough de docker/connect; Tab dispara sobre token que empieza con `-`. Pieza base: registro `commandFlags`. `View()` ahora colorea todo el buffer vía `lineStyles`; `handleTab` comparte el flujo 1/LCP/lista entre comando y flag. Spec `24-flag-highlight-complete.md` + build plan + código + tests + CHANGELOG + tracker en un commit.
- 2026-06-08: Unit 26 (addons-paths-from-conf) implementada tras el usuario reportar `echo.update.error: no modules found — configure addons paths with` modules --config`` en `habitta_prod`. Diagnóstico: Echo solo escaneaba carpetas del host junto al compose, pero esa instancia tiene los addons dentro del contenedor declarados en `addons_path` del `odoo.conf`. Solución: fallback automático que lee el `odoo.conf` del contenedor (`docker exec cat`), parsea `addons_path` y lista los módulos dentro del contenedor, persistiendo `addons_mode=conf`+rutas (`conf_path` default `/etc/odoo/odoo.conf`). Modo conf re-lee en vivo (auto-update); `modules --config` sigue fijando modo host (coexistencia). Nuevo `resolveModules` reemplaza las llamadas directas a `listAvailableModules`. Spec `26-addons-paths-from-conf.md` + build plan + config + código + tests + CHANGELOG + tracker en la rama `feat/unit-26-addons-paths-from-conf`. Build/vet/tests verdes; falta probar contra la instancia real.
- 2026-06-08: Unit 25 (filestore-in-container) implementada tras el usuario reportar `FileNotFoundError: /var/lib/odoo/filestore/habitta_prod/06/...` post-restore. Diagnóstico: el SQL restauraba bien pero el filestore se copiaba al host (`~/.local/share/Odoo`), fuera del alcance del contenedor. Confirmado que el archivo SÍ venía en el zip. Decisiones en form: `docker cp` al contenedor (ruta configurable, default `/var/lib/odoo/filestore`) + arreglar restore y backup. Spec `25-filestore-in-container.md` + build plan + config (`filestore_path`) + `docker.ExecAsRoot` + reescritura de filestore en `db.go` + limpieza de helpers de host + CHANGELOG + tracker en un commit.
